# CLAUDE.md — Bản đồ dự án SANJI / NEW88 Dashboard

> File này là BẢN ĐỒ để định vị nhanh code, tránh phải đọc lại toàn bộ `dashboard_v2.html` (4.779 dòng).
> Khi sửa: đọc bản đồ → nhảy tới vùng dòng liên quan → chỉ đọc vùng đó → sửa.
> **Số dòng sẽ lệch dần sau khi chỉnh sửa** — nếu lệch, `Grep` tên hàm/marker gần nhất để định vị lại, rồi cập nhật số ở đây.

## Tổng quan
- Dashboard 1 file HTML tĩnh cho đội vận hành cá cược (NEW88/SANJI).
- Hosting: **GitHub Pages** (repo `ttsanjinew88-boop/SANJI-quanlyne`, **PHẢI để public** — gói Free).
- Backend: **Supabase** (Postgres + Auth + Storage + Edge Functions). Anon key công khai nhưng an toàn nhờ RLS.
- File chính duy nhất: `dashboard_v2.html`. Không có build step.

## Quy tắc & lưu ý sống còn (đọc trước khi sửa)
- Chỉ **1 bot Telegram** duy nhất. Không tạo bot thứ 2.
- **Không để secret** (token/service_role) trong HTML. Chỉ anon key.
- Khi backup Storage: **loại bỏ 3 cột G/H/I** (`stripSensitiveCols`).
- Đơn có tích xanh ✅ (cột F chứa ký tự U+2705) **< 7000 không tính điểm/đơn**.
- User tự sửa format tin nhắn Telegram trong `index.ts` (biến `txt`) — đừng tự ghi đè.
- User **tự chạy SQL** và **tự deploy Edge Function**. Không tự làm hộ.
- **Đừng sửa file trực tiếp trên GitHub** khi Claude đang sửa local (gây lệch, phải `git pull --rebase`).
- Deploy: `git commit → push → chờ GitHub Actions`. Poll API `until grep '"conclusion"'`.
- **BUILD (chốt 24/07/2026)**: `dashboard_v2.html` là FILE SINH RA từ `src/` — **ĐỪNG sửa thẳng file này** (build ghi đè). `build.ps1` (root) ghép các mảnh bằng `.Replace` chuỗi thô (giữ CRLF, marker mỗi cái 1 dòng, file con không có newline thừa → byte-identical). `serve.ps1` tự chạy `build.ps1` trước khi phục vụ. **Phép thử vàng: build xong `git diff dashboard_v2.html` phải RỖNG** (SHA256 khớp). `deploy.yml` ĐÃ có step `./build.ps1` trước `upload-pages-artifact` (CI tự sinh lại), nhưng **vẫn nên commit cả `dashboard_v2.html`** để bản trong repo khớp với bản deploy. `upload-pages-artifact` lấy `path: '.'` ⇒ **mọi file trong repo đều được GitHub Pages phục vụ** (vd `assets/favicon-*.png`).
  - `src/dashboard_v2.src.html` (~856 dòng) — khung: gần như toàn bộ HTML (body/modal/login/tabs) + các dòng marker `//#include`. **JS đã tách hết ra `src/js/`.**
  - `src/styles.css` — toàn bộ CSS (khối `<style>`).
  - **JS khối T1** (thứ tự trong file gộp): `src/js/i18n.js` (song ngữ VI/EN — xem mục "Song ngữ") → `src/js/core.js` (util, cấu hình cột Nghi Ngờ, module `SB` Supabase, profile/quyền) → `auth.js` (AUTH đăng nhập + phân quyền + white-IP lúc login) → `admin.js` (rAdminPanel, 2FA, white-IP quản lý, Lịch Sử, xóa/xuất tháng) → `data-boot.js` (tháng/dataset/bootData, KO_OV/KO_AN localStorage, URL handler Telegram, progress UI, hàm chấm điểm) → `upload.js` (upload dropdown, parse Excel, helper Đơn Rút, cộng dồn add-mode) → `nav-donrut.js` (navigation sw/switchTool/rAll + module `DR` Báo Cáo Đơn Rút) → `render.js` (thống kê KM, render tab Dữ Liệu, Hiệu Suất Duyệt Đơn, Tổng Quan, Bất Thường, Hạn Mức) → `shift-rank.js` (Phân Ca, Công Việc, Xếp Hạng, màu FK).
  - **JS khối T2**: `src/js/bc.js` (module `BC`: parse, render, Tổng Hợp/Nghi Ngờ, gửi Telegram/Sheet) + `switchTool` (nay chạy theo mảng `TOOLS=['t1','t2','t3']` — thêm tab lớn chỉ cần nối thêm phần tử + nút `#tsb<N>`).
  - **JS khối T3**: `src/js/ntk.js` (module `NTK`: Lọc File NTK — xem mục riêng bên dưới).
  - **JS khối T5**: `src/js/exam.js` (module `EX`: Kiểm Tra Nghiệp Vụ — xem mục riêng bên dưới).
  - ⚠ Số dòng trong bảng "Bố cục" & "Bản đồ chức năng" bên dưới là theo FILE GỘP; khi sửa thì mở đúng file `src/js/*` tương ứng (grep tên hàm để định vị trong file con).

### Favicon (chốt 28/08/2026)
`assets/favicon-32.png` · `favicon-64.png` · `favicon-180.png` (apple-touch) — sinh từ logo NE gốc (`media.88new11.com/.../73d3f524….png`, 1254×1254). Bản 32/64 **cắt sát vào giữa 66%** để chữ NE còn đọc được ở cỡ tab trình duyệt (để nguyên logo đầy đủ thì ở 32px chỉ còn một đốm cam); bản 180 giữ nguyên toàn khung. Link nằm trong `<head>` của `src/dashboard_v2.src.html`, đường dẫn TƯƠNG ĐỐI (`assets/…`) để chạy đúng cả trên GitHub Pages project-site lẫn `serve.ps1`. Đổi logo thì tạo lại 3 file bằng System.Drawing rồi commit đè, không cần sửa HTML.

## Bố cục file `dashboard_v2.html` (số dòng theo FILE GỘP; sửa thì vào `src/`)
| Vùng | Dòng | Nội dung |
|---|---|---|
| CSS | 14–438 | `<style>`; biến theme ở `:root{` dòng **15** (galaxy dark) |
| HTML body | 439–981 | Toàn bộ giao diện (modal, login, tabs, các trang) |
| JS khối 1 (T1 – hệ thống chính) | 982–3788 | Logic hiệu suất nhân viên |
| HTML T2 (Báo cáo đại lý) | 3790–3999 | Tab gửi file đại lý |
| JS khối 2 (T2 – BC tool) | 4001–hết | Logic báo cáo đại lý |

### Biến theme CSS (`:root`, dòng 15)
`--bg --card --card2 --border --border2 --pu(#7c3aed) --pu2(#9f67ff) --bl(#3b82f6) --bl2 --cy(#06b6d4) --gr --go(#f59e0b) --re --pk --tx --mu --mu2 --vip-c(#06b6d4) --onl-c(#a78bfa)`
→ Khi thêm UI, dùng các biến này (KHÔNG hardcode màu vàng/teal của PROMAX).

### Banner đầu trang của 4 tab lớn (chốt 21/08/2026)
Cả 4 tab lớn đều mở đầu bằng một dải banner cùng kiểu (gradient tím-xanh `#1a1040→#0f0d2a→#0a1535`, logo NE 38px, tên tab 1.1rem, dòng "SANJI", ghi chú đẩy sang phải, nút VI/EN cuối cùng):
- T1 = `.hdr` (thêm chip tài khoản/Đăng xuất) · T2 = `.bc-tool .topbar` — 2 cái này có sẵn từ đầu, **không đụng**.
- T3/T4 = `.tool-hdr` + `.tool-hdr-title/.tool-hdr-sub/.tool-hdr-note` (styles.css cạnh `.hdr-date`). ⚠ T3/T4 nằm TRONG `.pg` (padding 20px 24px) nên banner phải `margin:-20px -24px 16px` mới tràn hết bề ngang — đổi padding `.pg` thì phải sửa theo.
- Nút ngôn ngữ của T3/T4 (`#btnLang3`/`#btnLang4`) nay nằm TRONG banner, **đã gỡ khỏi dòng `.sec-hdr`** bên dưới; dòng `.sec-hdr` giữ lại làm tiêu đề mục (T4 vẫn chứa cụm `#sopAdminTools`). Thêm tab lớn mới → dựng `.tool-hdr` tương tự + thêm id nút vào mảng trong `i18nSyncBtn`.

### ⚠ QUY ƯỚC NÚT (bắt buộc khi tạo chức năng mới — chốt 02/08/2026)
**KHÔNG viết inline-style cho nút.** Mọi nút mới PHẢI dùng bộ class chuẩn `.abtn` trong `styles.css` (cạnh `.upload-btn`) để đồng nhất kích cỡ + form chữ toàn dashboard:
- Base: `.abtn` (padding 8px 18px, .72rem, radius 8px, hover nhấc + brightness). Biến thể nhỏ trong bảng: thêm `.abtn-sm` (4px 12px, .62rem).
- Màu: **MẶC ĐỊNH nút chính/hành động thường = `.abtn-pu` (tím-xanh galaxy — ĐÂY là màu tiêu chuẩn, cứ dùng cái này trừ khi có lý do ngữ nghĩa rõ ràng)** · `.abtn-ok` (xanh lá — CHỈ cho lưu/tải/xác nhận) · `.abtn-danger` (đỏ — CHỈ cho xóa/khóa) · `.abtn-cy` (xanh ngọc — hạn chế, chỉ khi cần phân biệt trong cụm nhiều nút) · `.abtn-ghost` (viền trong suốt — nút phụ/Đóng). ⚠ ĐỪNG mặc định chọn `.abtn-cy`/màu lạ cho nút mới — nút chính LUÔN `.abtn-pu`.
- Ví dụ: `<button class="abtn abtn-ok">Lưu</button>`, `<button class="abtn abtn-sm abtn-danger">Xóa</button>`.
- `.upload-btn` (gradient tím, radius 8px) vẫn dùng cho nút header (Upload Excel, Tháng ▾) — KHÔNG đè inline lên nó (từng làm nút "Tháng ▾" lệch, đã bỏ). Cần thêm màu/biến thể thì bổ sung class `.abtn-*` mới, đừng inline.

#### BẢNG KÍCH CỠ CHUẨN — chỉ được dùng đúng 4 cỡ này (chốt 28/08/2026)
Lấy nút của **T1 Hiệu Suất Nhân Viên** làm chuẩn. Đã quét toàn dashboard bằng `getComputedStyle`: 0 nút nằm ngoài bảng.

| Cỡ | Class | Kích thước | Dùng cho |
|---|---|---|---|
| Tab cấp 1 | `.tab` | `12px 18px · .7rem` (cao 41px) | điều hướng trong tab lớn — xem "QUY ƯỚC TAB NHỎ" |
| Nút thường | `.abtn` (và `.upload-btn`) | `8px 18px · .72rem` | mọi nút hành động, nút trong hộp thoại, nút header |
| Nút chuyển chế độ | `.vt-btn` trong `.view-toggle` | `5px 14px · .67rem` | chuyển view CẤP 2 (Tổng Quan / Duyệt Đơn / Bất Thường…) |
| Nút nhỏ trong bảng | `.abtn abtn-sm` | `4px 12px · .62rem` | nút trong ô bảng, cụm nút phụ chật chỗ |

- Nút đóng **✕** của mọi hộp thoại: class **`.icon-x`** (16px, nền trong suốt) — đừng viết `style="background:none;border:none;font-size:16px"` nữa.
- Biến thể màu đủ bộ: `abtn-pu` (mặc định) · `abtn-ok` · `abtn-danger` · `abtn-cy` · `abtn-go` (vàng) · `abtn-bl` (xanh dương) · `abtn-ghost`.
- **Ngoại lệ CỐ Ý** (đừng "sửa cho đồng bộ"): `.tool-sw-btn` (thanh tab LỚN cấp 0, 15px 20px) · `.lang-opt` (cờ VI/EN) · `.pg-btn` (số trang) · `.otp-box` (ô nhập mã 6 số).
- ⚠ **Bẫy cascade ở T2**: `.bc-tool *{padding:0}` có specificity (0,1,1) nên **ĐÈ CẢ `.abtn`** (0,1,0) ⇒ nút chuẩn đặt trong T2 mất sạch padding. Đã vá bằng `.bc-tool .abtn/.abtn-sm/.vt-btn/.icon-x{padding:…}` ngay dưới dòng reset. **Thêm biến thể/cỡ nút mới thì phải khai thêm dòng tương ứng cho `.bc-tool`.**
- *Dọn 28/08/2026*: đã gỡ ~35 nút viết style thẳng trong thẻ (T1/T2 + `bc.js`/`render.js`) và xoá 5 class nút cũ trùng vai trò (`login-btn`, `shift-update-btn`, `btn-process`, `btn-reset`, `export-btn`) — đừng tạo lại class nút riêng, cứ dùng `.abtn`.

### ⚠ QUY ƯỚC TAB NHỎ — MỘT KÍCH CỠ DUY NHẤT (chốt 28/08/2026, bắt buộc)
**Mọi hàng tab nhỏ (cấp 1) bên trong MỌI tab lớn đều phải dùng class `.tab`, kích cỡ lấy từ T1 làm chuẩn: `padding:12px 18px · font-size:.7rem · font-weight:700 · letter-spacing:.06em · text-transform:uppercase · gạch dưới 2px` → cao đúng **41px**.** Trước đây mỗi tab lớn tự chế một cỡ (T2 `11px 22px/12px` + viền phải; T3/T5 dùng `.vt-btn` dạng viên nhỏ `5px 14px/.67rem`; T4 đè `.75rem/11px 18px` + không in hoa) nên nhìn lệch hẳn nhau — user chê 28/08/2026, đã gom hết về một chuẩn.
- **Thêm tab lớn mới**: bọc `<div class="tabs-wrap tabs-inline" id="…">` rồi render các mục bằng `<div class="tab">`. `.tabs-inline` = biến thể nằm trong trang (bỏ `position:sticky`, thêm bo góc + viền) — T1 dùng `.tabs-wrap` trần vì nó dính đầu màn hình.
- **ĐỪNG** tự đặt `padding`/`font-size` riêng cho tab, đừng quay lại `.vt-btn` cho cấp 1.
- **Phân cấp**: `.tab` = điều hướng CẤP 1 trong tab lớn (Dữ Liệu / Làm Bài / Quản Lý Đề…). `.view-toggle` + `.vt-btn` = chuyển view CẤP 2 bên trong một tab nhỏ (Tổng Quan / Duyệt Đơn / Bất Thường của tab Hiệu Suất KO, Hiệu Suất / Test Nghiệp Vụ của tab Xếp Hạng). Đừng dùng lẫn.
- ⚠ Chip/badge nhét trong `.tab` phải **thấp hơn 15px** (dòng chữ của tab) kẻo đội cả hàng tab cao lên — `.bc-tool .data-source-badge` phải giữ `line-height:1` (từng làm tab T2 cao 43px thay vì 41px).

### ⚠ QUY ƯỚC CHỮ TRÊN CHIP/NHÃN (chốt 23/08/2026 — áp cho MỌI chức năng mới)
**FORM CHỮ TIÊU CHUẨN của chip/nhãn nhỏ: `font-size ≥ .6rem · font-weight 700 · letter-spacing .05em · text-transform:uppercase (bằng CSS)`.**
- **CẤM VĨNH VIỄN form chữ cũ** `font-size .53rem + font-weight 800 + letter-spacing .08em` (từng dùng cho chip `.sop-lvl` "HẠN CHẾ" — user chê xấu, đã đổi 23/08/2026). Đừng tạo lại kiểu chữ này cho bất kỳ chức năng mới nào.
- **KHÔNG in hoa bằng JS** (`.toUpperCase()` trong chuỗi render) — i18n khớp NGUYÊN VĂN nên chuỗi in hoa sẽ **trượt từ điển** và kẹt tiếng Việt khi bật EN (đúng lỗi của chip `.sop-lvl` trước đây). In hoa LUÔN bằng `text-transform:uppercase`.

## Các TAB (HTML) và hàm render tương ứng
Điều hướng tab: `sw(pg,el)` (dòng 2638) → gọi `rAll()` (2795).

| Tab (nút) | id trang | HTML | Render chính |
|---|---|---|---|
| Dữ Liệu | `pg-data` | 582–670 | `rDataTab()` 2653 |
| Hiệu Suất KO | `pg-ko` | 672–797 | `rKO()` 3196 |
| Phân Ca | `pg-shift` | 799–883 | `rShift()` 3676 |
| Xếp Hạng | `pg-rank` | 913–928 | `rRank()` 3726 |
| Lịch Sử | `pg-log` | 930–937 | `rLogPanel()` 1752 |
| Quản Trị (admin) | `pg-admin` | 939–981 | `rAdminPanel()` 1424 |
| Báo Cáo Đại Lý (T2) | trong t2 | 3790–3999 | `switchTool()` 4003, `BC.*` |

## Bản đồ chức năng → hàm (số = dòng bắt đầu)

### Danh sách nhân viên (ROSTER — THÊM/BỚT động, chốt 30/07/2026)
- **KHÔNG còn hardcode** `FK_KEYS/FK_NAMES/FKVIP/FKONL/FK_COL/FK_SEARCH` (giờ là `let`, rỗng, rebuild bởi `applyRoster()`). Nguồn sự thật = `ROSTER` (mảng member `{key,name,group:'vip'|'onl',col,search,active}`) trong `core.js` cạnh `ROSTER_DEFAULT` (15 người gốc). `applyRoster()` suy ra mọi biến: active-only cho hiển thị/phân công; `FK_NAMES` giữ CẢ người `active:false` để render lịch sử không "undefined". `NAME2FK` cũng rebuild ở đây (đã gỡ khỏi shift-rank.js). `search` = chuỗi con nhận diện FK ở cột note Excel (dùng bởi `mfk`).
- **Lưu cloud RIÊNG TỪNG THÁNG (chốt 01/08/2026)**: report `type='roster', month='YYYY-MM'` = `{members:ROSTER}`. Sửa roster tháng nào CHỈ ảnh hưởng tháng đó. `bootData`/`loadHistMonth` gọi `await applyRosterForMonth(mk)` (data-boot.js) TRƯỚC `reconcileDataset` → nạp bản riêng của tháng, không có thì **kế thừa** tháng gần nhất trước đó (LAZY, không tự lưu), rồi tới bản `month='all'` cũ (tương thích ngược), cuối cùng `ROSTER_DEFAULT`. `applyRosterFromCloud(members|null)`: members rỗng/null **reset về mặc định** (tránh rớt roster tháng khác còn trong RAM) → `applyRoster` + `BC.renderFkChips`. `saveRoster(label)`: applyRoster + `reconcileDataset(D/KMD)` + render chip + upsert cloud vào `month=CUR_MONTH` + `rAll()`. ⚠ RLS `reports` phải cho phép ghi type 'roster' (user tự chạy SQL nếu bị chặn). *Di trú: các tháng cũ chưa có bản riêng vẫn hiện danh sách 'all' đã đông đủ; chỉ khi SỬA một tháng thì tháng đó mới tách ra.*
- `reconcileDataset(ds)` (core.js): thêm fk_data rỗng cho MỌI FK active còn thiếu + đồng bộ `ds.fkvip/fkonl` theo ROSTER → nhân viên MỚI hiện ngay ở tháng cũ mà không vỡ render. Gọi khi load don/km.
- **UI Tổng Quan** (Admin + Tổ Trưởng, gate `canManageRoster()` + `applyPerms` nút `#rosterEditBtn`): nút "👥 Quản lý nhân viên" mở modal `#rosterModal` (class `dr-modal`, đóng nền/Esc chung). Hàm ở render.js: `rosterOpenModal/rosterCloseModal/rosterRenderList/rosterAddMember/rosterRename/rosterToggleActive`. Thêm = nhập tên+nhóm+màu+mã Excel (key tự sinh `fk<ten>`, duy nhất). Xóa = **ẩn (active:false)**, GIỮ lịch sử (không xóa hẳn).
- **Sửa tên + mã Excel nhân viên (chốt 01/08/2026)**: nút "✎ Sửa tên/mã" mỗi dòng → `rosterRename(i)` (render.js) hỏi 2 prompt: TÊN rồi MÃ EXCEL (`search`). Khác thêm/ẩn (theo tháng): sửa áp cho **TẤT CẢ các tháng** — `renameMemberEverywhere(key,newName,newSearch)` (data-boot.js) duyệt mọi report `type='roster'` (mọi tháng + bản `'all'` cũ, bỏ `CUR_MONTH`), sửa `name` (+`search` nếu truyền) của member trùng `key` rồi upsert lại; tháng đang mở do `saveRoster` ghi; tháng CHƯA có bản riêng tự lấy giá trị mới qua kế thừa. `key` nội bộ & điểm số KHÔNG đổi. `search` lưu lowercase.
- **BC chip nhân viên phụ trách** render động từ roster: `BC.renderFkChips()` (bc.js) fill `#bcBtChips` (bất thường = nhóm VIP) + `#bcCuChips` (cược = tất cả active); gọi trong `openBtModal`/`openCuModal`. HTML chip cứng cũ đã thay bằng container rỗng.
- **⚠ Gửi báo cáo Telegram PHẢI dùng KEY roster, không ghép từ tên (chốt 11/08/2026)**: `sendBatThuong`/`sendCuocBatThuong` (bc.js) trước đây gửi `'fk'+tênChip.toLowerCase()`. Edge Function cộng điểm THẲNG vào mã nhận được (`an[cat][fk][day]`, index.ts ~300) còn bảng Bất Thường render theo `FK_KEYS` ⇒ **đổi tên nhân viên là điểm rơi vào mã lạ, biến mất khỏi hiệu suất** (CHAMY→SOLIS 05/08/2026: key vẫn `fkchamy` nhưng bot nhận `fksolis`). Nay dùng `BC._fkKey(name)` = `NAME2FK[TÊN]` (fallback ghép cũ). Chip vẫn truyền tên (hiển thị), chỉ lúc gửi mới quy ra key. Dữ liệu lạc cũ gộp bằng `supabase_merge_fk_solis.sql` (cộng dồn theo ngày `fksolis`→`fkchamy` mọi tháng, cả `abuse`+`mkt`, rồi xóa mã lạc — user tự chạy). Không cần deploy lại Edge Function.

### Song ngữ VI/EN (i18n — GĐ1 + GĐ2 XONG, chốt 13/08/2026)
- File `src/js/i18n.js` (nạp ĐẦU TIÊN, trước `core.js`). **Không sửa logic render**: từ điển lấy KHÓA = chính chuỗi tiếng Việt đang có trong HTML/JS (`I18N.EN`), engine dịch ở tầng DOM nên mọi bảng/thẻ do `r*()` sinh ra đều được dịch mà không đụng code cũ. Thêm chữ mới = thêm 1 dòng vào `I18N.EN`.
- Cơ chế: `trs(s)` (khớp nguyên văn → nếu trượt thì thử `I18N.RX` regex cho chuỗi có số, vd `"1.234 đơn"`); `i18nNode` lưu bản gốc ở `n.__vi` + dấu vết bản dịch `n.__out`; `i18nWalk` quét text node + `placeholder/title/alt`; `MutationObserver` dịch tiếp nội dung mới render (chỉ chạy khi EN); nhãn biểu đồ dịch bằng plugin Chart.js `i18nLbl` (canvas không có text node); `alert/confirm/prompt` được bọc, dịch TỪNG DÒNG.
- `setLang('vi'|'en')` lưu `localStorage.ui_lang` (mặc định **vi**), khôi phục nguyên văn từ `__vi`, rồi gọi lại `rAll()`/`BC.renderAll()` để vẽ lại chart. Nút chuyển: **3 tab lớn dùng chung một bộ** — `#btnLang` (header T1) · `#btnLang2` (topbar T2) · `#btnLang3` (sec-hdr T3, CSS `.sec-hdr .lang-sw{order:1}` đẩy ra sát mép phải sau đường kẻ `::after`). Cả 3 chỉ là `<div class="lang-sw" data-noi18n>` RỖNG trong HTML; `i18nSyncBtn()` (i18n.js) bơm cùng một markup 2 nút cờ **VI 🇻🇳 / EN 🇬🇧** (cờ là **SVG inline** trong hằng `I18N_FLAG`, không gọi mạng, không phụ thuộc font emoji) và tô sáng nút đang bật (`.lang-opt.on`). Bấm = `setLang('vi'|'en')` trực tiếp (không còn toggle mù); `i18nToggle()` giữ lại để tương thích. CSS `.lang-sw/.lang-opt/.lang-flag` ở `styles.css` cạnh `.upload-btn`. Thêm tab lớn mới → chỉ cần đặt 1 div `.lang-sw` id `btnLang<N>` rồi thêm id đó vào mảng trong `i18nSyncBtn`.
- ⚠ Vùng gắn `data-noi18n` (và nội dung `<textarea>` người dùng gõ) KHÔNG bị dịch; riêng `placeholder` của textarea VẪN dịch.
- `i18nRev()` (EN→VI, map ngược dựng lazy từ `I18N.EN`): dùng khi code cũ **lưu chữ đã dịch rồi ghi trả vào DOM** (vd `bc.js` lưu nhãn nút trước khi gửi Google Sheet) — không có nó thì ô đó kẹt tiếng Anh khi bấm về VI.
- **Phủ toàn bộ giao diện (GĐ1 + GĐ2)**: header/tab, đăng nhập + 2FA, Dữ Liệu (gồm Báo Cáo Đơn Rút), Hiệu Suất KO (4 view), Phân Ca + Công Việc, Xếp Hạng, Quản Trị, Lịch Sử, **toàn bộ T2 Báo Cáo Đại Lý** (Tổng Hợp / Danh Sách / Kiểm Tra / Nghi Ngờ + 2 modal báo cáo) và các `alert/confirm/prompt` chính của upload/admin/auth/BC. Phép thử: bật EN rồi quét text node còn dấu tiếng Việt trên **toàn `document.body`** → **0 sót**; hiệu năng chèn 3.000 dòng bảng BC ≈ 95ms.
- ⚠ **KHÔNG dịch (cố ý, đừng "sửa")**: header Excel/Google Sheet trong `BC.XLS_FIELDS`/`HDRS` (hằng JS, `.gs` khớp theo tiêu đề tiếng Việt), chuỗi xác nhận `XOA <tháng>` phải gõ tay, tên nhân viên/đại lý. Ngoài phạm vi: nội dung file Excel nguồn, tin nhắn Telegram (`index.ts`), Google Sheet (`.gs`).

### Tiện ích chung
`ha` 1006 (hex→rgba) · `nn` 1007 (số VN) · `hesc` 1009 (escape HTML) · `co/coL` 1045/1046 (config Chart.js) · `m31x24` 2308 · `hrs` 1044 (dải giờ).

### Supabase / Auth / Quyền
- `SB` (module) 1053: `saveReport` 1061, `uploadOriginals` 1067, `listReports` 1076, `loadReport` 1082.
- Profile/quyền: `CUR_PROFILE` 1100, `canView` 1108, `canEdit` 1109, `curPerms` 1383, `applyPerms` 1388, `roleOf` 1117.
- `logAction` 1111 (ghi audit) · `sessionExpired` 1125.
- IP: `fetchMyIP` 1131, `ipAllowed` 1141, `showIpBlocked` 1152.
- Login/2FA flow: `AUTH` init ~1160+ (đăng nhập, kiểm 2FA, ép thiết lập 2FA). QR: thư viện `qrcode-generator`.

### 2FA (Google Authenticator)
- **Ô nhập mã 6 số + phím Enter (chốt 18/08/2026)**: module `OTP` (core.js, grep `const OTP=`) dựng 6 ô rời từ `<div class="otp-wrap" data-for=".." data-submit="..">`. `data-submit` được tra qua `window` ⇒ `const AUTH={...}` (auth.js) nằm ở global **lexical** scope, KHÔNG có trên `window` ⇒ 2 ô `AUTH.verifyLogin2fa` / `AUTH.confirmEnrollLogin` **trượt âm thầm** (nhập đủ 6 số không tự gửi). Đã thêm `window.AUTH=AUTH;` cuối `auth.js` — thêm module mới kiểu `const X={}` mà dùng `data-submit="X.y"` thì phải phơi tương tự. Enter: handler `keydown` ở `document` (cuối core.js) — bấm Enter ở BẤT KỲ đâu (kể cả sau khi ô mất focus) sẽ gửi ô OTP đang hiện nếu đủ 6 số; bỏ qua khi đang gõ trong `<input>` khác/`<textarea>`. Chống gửi trùng bằng cooldown 1,5s theo mã trong `fire()` (auto-gửi lúc đủ 6 số + Enter ngay sau đó = 1 lần); `OTP.reset()` xóa `_lastCode` để nhập lại cùng mã vẫn gửi được.
`open2faModal` 1566, `start2faEnroll` 1583, `confirm2faEnroll` 1599, `disable2fa` 1611, `copy2faSecret` 1622, `admReset2fa` 1628 (TT/admin reset của người khác). Label hiển thị: "He Thong NE: TÊN - DD/MM/YY".

### Quản Trị (admin panel)
`rAdminPanel` 1424, `admLoadUsers` 1455, `admCreateUser` 1799, `admSetRole` 1770, `admSetAdmin` 1784, `admSetPermLevel` 1517, `admChangePw` 1533, `selfChangePw` 1548.
- **Xóa tài khoản (chốt 02/08/2026)**: cột "Xóa TK" trong bảng Danh sách tài khoản (chỉ ADMIN, ẩn với chính mình & admin khác) → `admDeleteUser(uid,uname)` (admin.js, cạnh `admReset2fa`): confirm + gõ lại đúng tên để xác nhận → POST `{action:'delete_user',target:uid}` tới `super-function`. Server (`handleAction`) chỉ cho ADMIN, chặn tự xóa/xóa admin khác, xóa `auth.users` (`sb.auth.admin.deleteUser`) + `profiles` + `login_security`, ghi audit. **Điểm/lịch sử theo roster KHÔNG mất** (chỉ xóa tài khoản đăng nhập, không đụng ROSTER). Cần deploy lại `super-function`.
White IP: `admLoadIps` 1644, `admAddIp` 1656, `admDelIp` 1674. Khóa TK: `admToggleLock` 1686.
Xóa/xuất tháng: `admFillDelMonths` 1696, `admExportMonth` 1707, `admDeleteMonth` 1730.
- **Xóa dữ liệu 1 NGÀY trong tháng (chốt 11/08/2026)**: hàng thứ 2 trong card `#admDeleteCard` — select `#admDelDay` + nút "Xóa dữ liệu ngày này". `admDelMonthChanged()` (admin.js, ngay trên `admExportMonth`) chạy khi đổi `#admDelMonth`: load `don`+`km` của tháng đó, gộp `dsDaysPresent` → đổ danh sách ngày (nhãn ghi rõ ngày đó có Duyệt Đơn / Khuyến Mãi / cả hai). `admDeleteDay()`: với mỗi loại có ngày đó → `dsSubtractDay` → `dsRecalcScores` (chỉ DON `roundV2`) → `dsRecalcDays` → `SB.saveReport` (KHÔNG đụng Storage/file gốc, không đụng shift/anomaly/bc); ghi `logAction('XÓA DỮ LIỆU NGÀY',…)`; `bootData()` nếu là `CUR_MONTH`. Dùng để sửa upload nhầm rồi upload lại ngày đó ở chế độ "Thêm ngày (cộng dồn)". ⚠ `dsSubtractDay`/`dsRecalcScores` (upload.js) nay duyệt `Object.keys(ds.fk_data)` thay vì `FK_KEYS` — xóa ngày ở THÁNG KHÁC (roster khác) hoặc nhân viên đã ẩn vẫn trừ đúng, không để tổng = 0 mà ô FK còn số.

### Tháng / nạp dữ liệu
`curMonthKey` 1834, `emptyDataset` 1838, `bootData` 1871, `normMonth` 1915, `dispMonth` 1917.
Lịch sử tháng: `toggleHistMenu` 1995, `loadHistMonth` 2025.

### Upload & parse Excel (Duyệt Đơn / Khuyến Mãi)
- Chọn kiểu upload: `toggleUploadMenu` 2234, `pickUploadType` 2238, `processFiles` 2275.
- Pipeline: `initAccum` 2309 → `processChunks` 2337 (parse từng dòng) → `mergeAccum` 2438 → `finalizeResult` 2587.
- Chế độ cộng dồn theo ngày: `applyAddMode` 2543, `dsAddInto` 2480, `dsSubtractDay` 2508, `dsRecalcDays` 2528, `dsDaysPresent` 2473.
- **Lọc ngày lẻ ở ranh giới (chốt 25/07/2026)**: nền tảng xuất file cắt ngày theo đồng hồ KHÁC giờ ghi ở cột T (GMT-4), nên vài đơn sát nửa đêm 22→23 lọt vào file "ngày 23" rồi bị xếp về ngày 22 (đúng theo quy tắc `pd.getDate()`) → ép user thay thế cả ngày 22. Fix: `filterUploadDays(aoa,target)` + `_dayOfRow(row,target)` (đầu `upload.js`, mirror parse ngày của processChunks) — CHỈ chạy khi `window._uploadMode==="add"`. Đếm số đơn theo ngày, GIỮ ngày có `count>=5% ngày lớn nhất HOẶC >=100 đơn`, BỎ phần còn lại (mẩu lẻ); hiện `confirm` liệt kê giữ/bỏ, hủy = dừng upload. File 2 ngày thật (vd 22+23) vẫn giữ CẢ HAI & cộng dồn bình thường. Hook trong `processFiles` ngay sau `sheet_to_json` (dùng `useAoa`).
- **Chống MẤT ĐƠN do roster (chốt 10/08/2026)** — 3 sửa sau khi phát hiện JADE ngày 08/08 mất 251 đơn (385 điểm) & SAM/REVOR/LEWIN = 0:
  1. `mfk` (data-boot.js) chọn mã **KHỚP DÀI NHẤT** thay vì người đầu tiên khớp theo thứ tự roster (mã ngắn không "ăn" đơn người khác, vd `sam` ⊂ `fkolsam`).
  2. `applyAddMode` gọi `reconcileDataset(base)` TRƯỚC `dsAddInto`, và cờ `legacy` không còn dựa vào `FK_KEYS[0]` (người mới thêm làm báo nhầm "dữ liệu cũ"). `reconcileDataset` (core.js) nay tạo cả `hbd7/cbd7` rỗng. **Bug cũ**: `dsAddInto` có `if(!b||!a)return` → nhân viên thêm SAU lần upload đầu của tháng bị bỏ âm thầm ở MỌI lần cộng dồn.
  3. `initAccum` có `nd._unk`, `noteUnknownFk(nd,note)` (data-boot.js) gom mã `fk...` trong ghi chú không khớp ai; `mergeAccum` cộng dồn; `finalizeResult` hiện **confirm cảnh báo** (ngưỡng ≥10 đơn/mã, liệt kê top 10) cho phép Hủy để sửa roster rồi upload lại. `_unk` bị xóa trước khi lưu cloud.
  ⚠ Đổi "Mã Excel" (search) / ẩn nhân viên **không** đổi điểm đã lưu, nhưng làm hỏng MỌI lần upload sau đó.
- Lưu: `saveMonthData` 2535, `cloudSaveKO` 1939, `stripSensitiveCols` 1926.
- Cột Duyệt Đơn (0-based): B(1)=lần đầu/thứ, D(3)=member, F(5)=Cấp độ(✅), J(9)=submit GMT-4, P(15)=amount, R(17)=status, T(19)=process GMT-4, U(20)=FK note. GMT-4→GMT+7 cộng 11h (chỉ xoay GIỜ; NGÀY chia theo GMT-4 — user chỉ upload các ngày đã trọn GMT-4).
- **Chấm điểm DON (roundV2, 20/07/2026)**: chỉ nhận 3 trạng thái qua `donStatusKind` (cạnh `gsc`) — "đã rút tiền" / "đã trả lại|trả về" (+1) / "đã từ chối" (0 điểm, VẪN đếm đơn); dòng trạng thái khác BỎ HẲN (lọc đầu nhánh DON trong `processChunks`). `gsc` giữ nguyên bảng điểm. **Làm tròn**: ô `hbd7` lưu THÔ (có 0.5); `dsRecalcScores` (ngay trên `dsRecalcDays`) suy MỌI số hiển thị từ ô thô — ceil RIÊNG từng ngày, `total_score` = Σ các ngày đã ceil ⇒ upload cả tháng 1 lần / cộng dồn từng ngày / thay ngày trùng đều ra CÙNG kết quả (đã test invariance qua preview). Dataset mới có cờ `roundV2:true`; add-mode vào tháng cũ thiếu cờ → alert bắt "Thay thế cả tháng" 1 lần. KM giữ cách chốt cũ (điểm nguyên, không đổi). Hiển thị hbd7 thô trong `rDay`/`fkHr`: `.map(Math.ceil)`. **Bug đã sửa kèm**: `processChunks` trước đây KHÔNG cộng `nd.fk_data[fk]._cbd7` (ô đếm đơn ngày-giờ của FK luôn 0 — làm dsSubtractDay không trừ được total_count khi thay ngày, và làm roundV2 báo "Không nhận diện được dữ liệu"); đã thêm `._cbd7[hbdDay-1][h7]++` cạnh `._hbd7`.

### TAB Dữ Liệu (Ngày/Tháng/Giờ + Báo Cáo Đơn Rút)
- Nguồn dữ liệu: `dataSrc` 2643 ('don'/'km'), `sds` 2645 (đổi nguồn), `curDataSet` 2652.
- Sub-tab Duyệt Đơn: `donSub` 2644 ('time'|'donrut'), `setDonSub` 2651, `rDataTab` 2653.
- **Báo Cáo Đơn Rút** (port đầy đủ từ PROMAX, **SESSION-ONLY** — không lưu cloud, F5 là mất): module `DR` (grep `const DR={`) giữ `DR.raw/DR.data/DR.brackets/DR.sortBy` trong RAM. Có ô thả file/dán/chọn file RIÊNG (`drDrop/drFile/drPick`, wire bằng IIFE cuối module) — độc lập với upload chính. Chức năng: sửa mốc tiền (`drOpenBrModal/drApplyBr/drResetBr` + modal `#drBrModal`), sort (`drSort` total/first/name), tìm FK+cấp độ. Thẻ FK gọn (chỉ header) → bấm mở **popup** `drOpenFk`/`drCloseFk` (modal `#drFkModal`, `DR.fkModalHTML`): mốc tiền drill-down `drToggleBr` + Cấp độ thành viên + **bảng Tất cả đơn kèm ID đăng nhập (o.m) & Cấp độ TV (o.lv)**. Đóng bằng ✕ / nền tối / Esc. **Cột Số Thứ Tự (mã đơn, cột A file)**: `DR.build` lưu `o.stt=r[0]` (mã đơn dài ~8 số, vd 29597092 — duy nhất) cho MỌI đơn; hiển thị cột "Số thứ tự" (màu cy) trong `ordersHTML` (drill-down mốc tiền, sau cột Hội viên·FK), `fkModalHTML` (bảng Tất cả đơn) và panel đối chiếu TT. **Xin ý kiến TT (Telegram) — đối chiếu ĐƠN >150k với mã STT trong text** (chốt 23/07/2026): mục đích = kiểm nhân viên đã hỏi ý kiến TT cho đơn rút lớn chưa. Ô dán text tĩnh trong stats-row (`#drTgText` + `drTgCheck`); user copy tin nhắn Telegram (chứa Số thứ tự đơn) dán vào. `drTgCheck` → `DR.parseSttSet` quét mọi số ≥5 chữ số trong text → `DR.tgStts` (Set, normalize bỏ 0 đầu qua `DR.sttKey`). `DR.tgCheck` gom MỌI đơn có `a>=150000` (big), mỗi đơn `sttHit = tgStts.has(sttKey(o.stt))`; sort đơn CHƯA khớp lên đầu rồi tiền giảm dần. `DR.renderTg` panel `#donrutTgPanel` (8 cột: # / Số thứ tự / FK duyệt / Hội viên / Số tiền / GMT+7 / Ngày / Trạng thái): đơn khớp = ✔ xanh, đơn STT KHÔNG có trong text = **⚠ CHƯA XIN Ý KIẾN** (đỏ, tô nền, xếp trên cùng) → cảnh báo nhân viên (FK duyệt) chưa xin ý kiến. Panel chỉ hiện sau khi dán text (cần `tgStts`). `DR.parseTg`/`tgMsgs`/`parseMoney` giữ lại nhưng KHÔNG còn dùng cho panel. Paste vào `drTgText` được loại khỏi handler dán Excel toàn cục. `DR.build` gộp gbr + fks{levels,brackets,orders}, LƯU MỌI đơn. Helper chung: `brLabelOf`, `fkLimitNum`, `fmtDurSec`, hằng `DR_DEFAULT_BR`. `rDonRut()`=`DR.render()`. Áp quy định ✅<7000. CSS `.dr-*` ở cuối khối style. **Đã gỡ donrut khỏi pipeline chính** (initAccum/processChunks/mergeAccum/dsAddInto).
- Thống kê KM: `rKmStat` 2747. Ngày/tháng: `bldDayBtns` 2804, `rDay` 2808, `rMonth` 2840, `rKoDaily` 2960.

### NHẮC NHỞ TELEGRAM — nguồn thứ 3 của tab Dữ Liệu (chốt 25/08/2026)
- Port từ web app Apps Script "Bot Nhắc Nhở Telegram" của user (giữ nguyên bố cục hàng nhắc, 4 chế độ, công tắc, "gửi thử ▶", thanh trạng thái động cơ). Bỏ mật khẩu riêng + Google Sheet + Script Properties → dùng đăng nhập/2FA và Supabase sẵn có. File `src/js/remind.js` (module `RM`, `window.RM=RM`), CSS `.rm-*` cuối `styles.css`, HTML `#data-remind` trong `#pg-data`.
- **Vị trí**: nút thứ 3 `#rmSrcBtn` cạnh Duyệt Đơn / Khuyến Mãi → `sds('rm')`. `rDataTab` có nhánh sớm cho `dataSrc==='rm'` (ẩn `#dataBody`, gọi `rmPaneShown(true)` + `RM.boot()`) — nguồn này KHÔNG có dataset tháng nên đổi tháng không ảnh hưởng. Quyền: **ADMIN + Tổ Trưởng**, ẩn nút trong `applyPerms` + `RM.canEdit()` chặn ghi + Edge Function kiểm lại `_role` (không chỉ ẩn nút).
- **Lưu**: `reports` type **`tgremind`** month `'all'` = `{groups:[{id,name,chatId,topicId,enabled,reminders:[{id,mode,time,content,enabled}]}]}`. Type **`tgremind_tick`** month `'all'` = `{at}` do Edge Function ghi mỗi phút (thanh trạng thái đọc để biết động cơ còn sống).
- ⚠ **`RM.boot()` chỉ render SAU khi nạp xong** (cờ `loading`) — bản đầu render trước rồi mới nạp, thao tác trong lúc chờ bị bản cloud ghi đè (đã vấp khi thử). Đừng gọi `RM.render()` trước `RM.boot()`.
- **1 bot, nhiều nhóm**: token KHÔNG bao giờ có trong HTML (secret `TG_BOT_TOKEN`, dùng chung `super-function`). `chat_id`/`topic_id` từng nhóm lưu trong DB (không nằm trong repo public, thêm nhóm không cần deploy lại). Không đụng `TG_G1/G2/G3`.
- **Động cơ = `supabase/functions/tg-remind/index.ts`**, deploy `--no-verify-jwt` (pg_cron gọi không có JWT; hàm tự kiểm `x-cron-key` = secret `TG_CRON_KEY`, hoặc JWT người dùng cho `action:'check'|'test'`). pg_cron `* * * * *` → pg_net POST. Chống gửi trùng: bảng `tg_remind_sent(day_key,group_id,rem_id,tag)` khóa chính — **chèn TRƯỚC khi gửi**, gửi hỏng thì xóa dòng để lượt sau thử lại (dung sai `TOL_MIN=2` phút).
- **Lịch sử = tab Lịch Sử CHUNG (chốt 25/08/2026)**: bản đầu có bảng `tg_remind_log` + tab con "Lịch Sử Nhắc Nhở" riêng — user thấy không cần, đã **gỡ hẳn** (bảng, tab con, `RM.setView/loadLog/fmtTime`, CSS `.rm-log*`). Nay `RM.log(detail)` gọi thẳng `logAction('NHẮC NHỞ',detail)` → `audit_log`. Động cơ **CHỈ ghi khi GỬI HỎNG** (`logFail`, action `NHẮC NHỞ LỖI`, username `BOT NHẮC NHỞ`, `user_id` null) — mốc nhắc chạy mỗi giờ mà ghi cả lần thành công thì lấp kín tab Lịch Sử chung; tin gửi được đã nằm sẵn trong Telegram.
- ⚠ Khi thêm khóa i18n cho tính năng mới, **kiểm trùng trước** (`grep '"Chuỗi":'`) — object literal trùng khóa thì bản SAU đè bản TRƯỚC, âm thầm. Đã lỡ tạo trùng "Trạng thái"/"Loại"/"Kết quả"/"Thao tác"/"Thời gian (GMT+7)" rồi phải dọn.
- **Múi giờ khóa cứng GMT+7**: server cộng 7h rồi đọc `getUTC*` (Deno chạy UTC — không tin giờ máy chủ), giống `sendReport`. `dueSlots` port nguyên logic Apps Script: `hourly` (phút 0–59, tag `h<H>`) · `interval` (N giờ, `H%N===0`, phút :00) · `daily`/`exact` (`HH:mm`, nhiều mốc cách nhau dấu phẩy, tag `t<HHmm>`). Đã test: 22:31 khớp mốc 22:30, 22:33 thì không, 22:29 không gửi sớm.
- SQL: **`supabase_remind_setup.sql`** (2 bảng + policy + thêm `'tgremind'` vào `can_write_report` cho Tổ Trưởng + cron job). User phải sửa `<PROJECT_REF>` và `<TG_CRON_KEY>` trước khi chạy. Tạm dừng toàn bộ: `select cron.unschedule('tg-remind-tick');`
- i18n: nhãn có trong `I18N.EN`; chuỗi kèm số (trạng thái động cơ, "N mốc nhắc · M đang bật") dùng `I18N.RX`. Nội dung tin nhắn / tên nhóm / ô chọn nhóm gắn `data-noi18n`. Đã quét EN trong `#data-remind` → 0 chữ Việt sót.

### Rà soát 11/08/2026 — 3 lỗi đã sửa
- `perfEdit` (render.js) chỉ ghi `day_scores` ⇒ lần "Thêm ngày (cộng dồn)" kế tiếp `dsRecalcScores` suy lại từ `hbd7` và **XÓA MẤT số chỉnh tay**. Nay ghi ngược vào `fd.hbd7[ngày]` (co giãn theo tỉ lệ giờ, bù sai số dấu chấm động vào ô lớn nhất), dựng lại `ds.hbd7` = Σ các FK rồi `dsRecalcScores`+`dsRecalcDays`. KM giữ nguyên cách cũ.
- `captureUrlAction` (data-boot.js) lọc `FK_KEYS.includes(fk)` **lúc tải trang** — khi đó roster tháng chưa nạp (còn `ROSTER_DEFAULT`) ⇒ nút Xác Nhận Telegram của nhân viên MỚI bị bỏ âm thầm. Nay chỉ kiểm định dạng `^fk[a-z0-9_]+$`, kiểm tên thật trong `processUrlAction` (báo lỗi rõ nếu không có trong roster).
- `applyShiftData` chỉ chạy `if(shift)` ⇒ mở tháng chưa có phân ca thì **giữ nguyên phân ca tháng trước** (và có thể lưu đè sang tháng mới). Nay luôn gọi, reset `shAssign` về null trước khi áp.

### TAB Hiệu Suất KO
`rKO` 3196 điều hướng view (`koView`, `skv` 2641). Các view:
- Tổng quan: `rKoOverview` 3095, bảng thưởng/phạt `buildOvTable` 3018, `calcBonusPenalty` 2995, `ovFootHtml` 3074.
- Duyệt Đơn (view `daily`, `rKoDaily`): thẻ FK do `fkGroupCardHtml` render — **GỌN thành 1 hàng/nhóm** (CSS `.fk-grid-compact` grid-auto-flow:column + `.fk-card-mini`/`.fkm-name/.fkm-score/.fkm-sub`; 4 lưới `perfVip/OnlGrid` + `kmVip/OnlGrid` đổi class sang `fk-grid-compact`). Bấm thẻ → `rPerfDetail` mở **POPUP** `#koChartModal` (class `dr-modal`, `koCloseChart`, canvas `koCmCh`) thay vì panel `#koDp` cũ (giữ trong DOM nhưng KHÔNG dùng). Đóng bằng ✕/nền/Esc (dùng handler `.dr-modal` chung của DR).
- Hạn mức: `LIMITS` 3245, `rKoLimit` 3246, `limSet` 3302, `_saveLimits` 3312. **Nút "⇄ Chuyển Hạn Mức Duyệt"** (chốt 02/08/2026): `#limCopyBtn` (`.abtn abtn-pu` — màu chuẩn galaxy) trong header `sec-hdr` view Hạn Mức (`ko-limit`, src html) → mở **popup chọn tháng đích** `#limCopyModal` (class `dr-modal`, đóng nền/Esc/✕ dùng handler chung). `limOpenTransfer()` (render.js, cạnh `inheritLimitsIfEmpty`): dựng danh sách tháng đích = các tháng đã có report + 6 tháng kế tiếp `CUR_MONTH` (bỏ chính `CUR_MONTH`), mỗi tháng 1 dòng nút (`.abtn abtn-ghost`) kèm nhãn "đã có hạn mức/trống". Bấm 1 tháng → `limTransferTo(mk)`: CHUYỂN toàn bộ hạn mức (chỉ field `limit`) của `CUR_MONTH` SANG tháng đó — MERGE (giữ PCQ sẵn có của tháng đích), confirm nếu tháng đích đã có hạn mức, upsert cloud + `logAction` + đóng modal. `limCloseTransfer()` đóng. Khác `inheritLimitsIfEmpty` (LAZY, kéo TỪ tháng trước vào tháng đang mở, không lưu) — nút này ĐẨY từ tháng đang mở SANG tháng khác & LƯU luôn. Gate `canEdit('ko')`. **Sao chép hạn mức sang tháng mới** (chốt 24/07/2026): `inheritLimitsIfEmpty(mk)` (cạnh `_saveLimits`) — nếu tháng đang mở CHƯA có report `limits`, `listReports` tìm tháng `limits` gần nhất `<mk`, load & copy **chỉ field `limit`** (không copy `pcq`) vào `LIMITS` trong RAM (LAZY: không tự lưu). Gọi trong `bootData` + `loadHistMonth` ngay sau `LIMITS=lm||{}`. Khi có người sửa → `limSet`→`_saveLimits` lưu cả bộ vào tháng đó ⇒ tháng sau kế thừa từ tháng vừa sửa.
- ~~Vượt hạn mức~~: **ĐÃ XOÁ view/tab riêng**. Cảnh báo vượt hạn mức giờ nằm TRONG Báo Cáo Đơn Rút: `DR.overLimitOrders` (amount > `fkLimitNum(fk)`) + `DR.renderOverLimit` (panel đỏ `#donrutOverLimit` + thẻ tổng thứ 5). Dùng `LIMITS` nạp sẵn ở boot.
- Bất thường: `KO_AN` 2081, `rKoAnomaly` 3169, `anSet/anGet/anTotal` 2100–2112, lưới `buildAnGridAbuse` 3115 / `buildAnGridMkt` 3140.
- KO_OV (cộng/trừ/ghi chú): `KO_OV` 2058, `ovGet` 2071, `ovSet` 2072, `_saveKoOvCloud` 2064.

### TAB Phân Ca / Công Việc
`rShift` 3676, `rShiftPanel` 3328, `sshv` 3344. Phân công: `WORK` 3355, `rWork` 3363, `wkSet` 3656, `wkAutoAssign` 3491, `wkAssignCore` 3438, `wkRebalanceFrom` 3502. OFF: `wkOffOpen` 3516, `wkOffConfirm` 3533. Dán Excel: `openPasteModal` 3580, `applyPaste` 3596.
- **Mã SN (sinh nhật OFF — chốt 30/07/2026)**: `WK_CODES` thêm `SN` (hồng #ec4899); `isOffish(v)` = OFF||SN. SN = nghỉ trọn ngày, xử lý y hệt OFF: `wkAssignCore` giữ nguyên (không xóa) + loại khỏi `avail`; `wkRebalanceFrom` không tính SN là "có plan"; thống kê `rWork` đếm SN **gộp vào cột OFF**; `wkTotalDays` không tính (chỉ KM/DD/HT). Dán Excel nhận SN tự động (qua `WK_CODES[v]`). SN KHÔNG dùng cho luồng "báo cáo OFF đột xuất/chuyển ngày" (đó vẫn set 'OFF').

### TAB Xếp Hạng
`rRank` 3726 (ẩn tên ngày 1–25 tháng hiện tại), `sFk` 3755, `rDC` 3756. Màu FK: `toggleFKColorPicker` 3769, `updateFKColor` 3779.
- **2 view (chốt 26/08/2026)**: `.view-toggle` `#rkViewToggle` → `srkv(v)` (shift-rank.js, ngay trên `rRank`) đổi giữa `#rk-hs` (ảnh cúp VIP/ONL theo điểm duyệt đơn/KM — nguyên như cũ) và `#rk-test` (xếp hạng bài test, gọi `EX.renderRank('rk-test',CUR_MONTH)`). Biến `rkView`; `rRank()` thoát sớm sang nhánh test nếu đang ở view đó (đổi tháng là tự nạp lại BXH test của tháng mới).
- **BXH test dùng ẢNH CÚP giống T1 (chốt 28/08/2026)**: `EX.rankHtml` (exam.js) nay dựng **ảnh `EX.RANK_IMG`** (cùng URL với `mkTrophyRow` của shift-rank.js) + 3 tên đặt tuyệt đối theo `EX.RANK_SLOT` (toạ độ % copy y hệt mkTrophyRow — **đổi ảnh phải sửa cả 2 nơi**). Điểm/tỷ lệ **KHÔNG in lên ảnh**: bảng bên dưới chạy **từ hạng 1**, 3 dòng đầu thay số bằng 🥇🥈🥉 + class `.ex-top1/2/3` (CSS cạnh `.ex-tbl .ex-nm`: nền mờ + vạch màu `inset box-shadow`, màu khớp màu tên trên ảnh). Không áp luật "ẩn tên ngày 1–25" của T1 (điểm test chấm xong là chốt). CSS `.podium-row`/`.pod-*` lại thành **mồ côi** — đừng tưởng nó đang được dùng. *Đã mô phỏng 5 bố cục cho user chọn 28/08/2026; phương án bị loại: 3 thẻ tóm tắt dưới ảnh, và in điểm chồng lên/dưới bệ cúp.*

### Telegram (deep-link toast trong HTML)
`PENDING_URL` 2117, `showUrlToast` 2128, `processUrlAction` 2154. Chấm điểm THẬT nằm ở Edge Function (server quyết định), không ở HTML.

### T2 — Báo Cáo Đại Lý (BC tool)
`switchTool` (grep). Module `BC`: parse file, render `renderAll`, phân trang, Tổng Hợp (20 cột), Nghi Ngờ (`renderSuspects`, cột ẩn/hiện `SUSPECT_VIS`/`toggleSuspectCol` ~1109+). Xuất Excel `genExcel` (SpreadsheetML; style `h/d/a` + **`m`** cam tô + **`r`** đỏ nhạt Âm/Dương). Gửi Telegram report qua Edge Function.
- **Tô ô nghi ngờ — Kiểm Tra Đại Lý** (SESSION-ONLY, F5 mất): `BC.MARKS` (agent→Set "rowIdx|field"), `mkSet/mkAttr/markCell`; td trong `loadAgent` bấm để tô. CSS `.bc-mkc/.bc-mk/.bc-mk-ad` (cạnh `.bc-cu-trigger` ~358). `sendBatThuong` truyền `mkSet(agent)` → `genExcel(rows,agent,marks)` tô đúng ô trong file Telegram (cam #F4B084, Âm/Dương #FFC7CE). `BC.XLS_FIELDS` = thứ tự 23 cột dùng chung cho Excel + Google Sheet.
- **Nghi Ngờ**: tab mở cho **ADMIN + Tổ Trưởng** (`applyPerms` ~1478 + gate trong `renderSuspects`); đánh dấu/bỏ đại lý nghi ngờ vẫn chỉ ADMIN (`toggleSuspect`).
- **Phân cách GIAI ĐOẠN báo cáo** (chốt 24/07/2026): mỗi đợt báo cáo chia Giai Đoạn 1 / 2 / Cuối. `BC.SUSPECTS` xen phần tử phân cách `{sep:true,at,by}` (KHÔNG có `dai_ly`); mỗi sep KẾT THÚC 1 giai đoạn. Helper: `suspectCount()` (đếm bỏ sep), `hasSuspectSep()`, `suspectSegments()` (trả `[{items:[{s,idx}],sepIdx,label}]`; label = "Giai Đoạn N" cho các giai đoạn trước, giai đoạn cuối = "Giai Đoạn Cuối"; 1 segment ⇒ "Giai Đoạn 1"). `items[].idx` = index thật trong `SUSPECTS` (dùng cho `suspectMark`/`suspectToggleDetail`). Nút **➕ Phân cách giai đoạn** (`#bcSusSepBtn`, ADMIN-only, hiện trong `applyPerms` cạnh `bcSusSheetBtn`) → `addSuspectSep()` push sep vào cuối (chặn double-sep & danh sách rỗng); mỗi ranh giới có nút **✕ Xóa phân cách** (ADMIN) → `removeSuspectSep(sepIdx)` splice. `renderSuspects` tách `suspectCardHtml(s,idx)` ra riêng, render header "▶ Giai Đoạn" + card + divider theo `suspectSegments()`. **Gửi Sheet theo giai đoạn**: nút giờ gọi `clickSheetBtn()` — nếu có sep mở menu `#bcSusPhaseMenu` (mỗi giai đoạn + "Tất cả", đóng bằng mousedown ngoài), không có sep gửi thẳng như cũ. `sendSuspectsToSheet(phase)` nhận `'all'`/index giai đoạn → chỉ gửi `list` của giai đoạn đó (server .gs không đổi). Sep lưu cloud chung `{list}` (tương thích ngược: record cũ không có sep = 1 giai đoạn). Tô ô LƯU CLOUD: `BC.suspectMark` ghi `s.cellMarks` (["rowIdx|field"]) vào record `suspects`. Nút `#bcSusSheetBtn` (TT trở lên) → `BC.sendSuspectsToSheet`: POST JSON `{token,by,blocks:[{agent,rows:[{v:{field:value},mk:[field]}]}]}` (Content-Type text/plain tránh CORS preflight) tới `BC.GS_WEBAPP_URL` (ĐÃ điền URL Web App user deploy 18/07/2026), token `BC.GS_TOKEN='sanji-bc-2026'` phải trùng file .gs. Tiền gửi dạng chuỗi VN (`BC.fmt`).
- `google_sheet_baocao.gs` BẢN 2 (trong repo; user tự dán vào Apps Script của Sheet đích gid `1340440362`, hướng dẫn ở đầu file): `doPost` khớp field theo TIÊU ĐỀ HÀNG 1 (`HEADER_FIELD`, không còn cột Cổng/Khu vực), mỗi đại lý 1 khối cách 1 dòng trống (cách cả hàng tiêu đề) + merge cột Đại lý + viền ngoài khối MEDIUM, Tahoma 9 đậm, nền: tiêu đề `#F8CBAD`, ô Đại lý `#F4B084`, ô tô `#FFE599`, Âm/Dương tô `#FFC7CE`, **Cấp bậc ĐỔI ĐẦU (`isDoiDau_`: "đổi/đối đầu"/DD-) TỰ ĐỘNG `#FFC7CE`**, thường `#FFF2CC`, ô trống điền "-"; dashboard gửi Thiết bị dạng "Máy Tính"/"Điện Thoại"; `monthlyRotate` trigger 06:00 ngày 1 hằng tháng — nhân bản sheet thành "Báo cáo đại lý ngoài tháng M - YYYY" (M = tháng vừa kết thúc) rồi breakApart + xóa dữ liệu chừa dòng 1; `setupTrigger` chạy tay 1 lần. **Sửa .gs xong user phải Deploy → Manage deployments → ✎ → New version** (URL không đổi).

### T3 — LỌC FILE NTK (nhiều tài khoản, chốt 14/08/2026)
- Tab lớn thứ 3 (`#t3`, nút `#tsb3`), **SESSION-ONLY**: toàn bộ trong RAM, F5 là mất, KHÔNG lưu cloud, KHÔNG cần SQL/Edge Function. **Mọi vai trò đều dùng được** (không gate quyền).
- Port từ Apps Script `locNhomNTK` (script rời của user, không nằm trong repo). Công dụng: gom nhóm theo **chuẩn_hóa(Tên thật) + IP**; nhóm có **≥2 TÀI KHOẢN KHÁC NHAU** = nghi ngờ 1 người nhiều TK → sinh câu lệnh xử lý dán sang hệ thống nghiệp vụ.
- Loại bỏ trước khi gom: thiếu ID/Tên/IP · IP bắt đầu `104.` (`NTK.IP_SKIP`, dải proxy/CDN) · cấp độ nằm trong `NTK.BANNED_RAW` (11 nhóm LD/CC/NTK/DD/QS…). Số dòng bị loại từng loại hiện ở card "Thống kê dòng bị loại" (không bỏ âm thầm).
- Hàm chính trong `src/js/ntk.js`: `loadFile` (thả/dán/chọn file riêng, độc lập upload chính) → `process` (lọc + gom, chia lô `CHUNK=5000`, có thanh tiến trình) → `render` (phân trang **theo NHÓM**, `PS=25`) · `exportCsv` (CSV + BOM UTF-8) · `copyCmd` · `toggleSort` · `clearAll`.
- **5 lỗi của bản Apps Script đã sửa khi port** (giữ nguyên comment đầu file `ntk.js`): (1) `dd/mm/yyyy` bị `new Date()` đọc thành `mm/dd` kiểu Mỹ → sai "TK nạp đầu"; (2) gom nhóm đếm **số DÒNG** thay vì **số ID phân biệt** → 1 TK nạp nhiều lần thành nhóm giả + câu lệnh rỗng; (3) `minMatches` khai báo rồi không dùng → chọn bừa hàng tiêu đề; (4) thiếu cột Cấp độ → bộ lọc cấm vô hiệu **âm thầm**, nay BÁO LỖI; (5) `afterList` lọc trùng ở câu lệnh nhưng vẫn in dòng → bảng lệch câu lệnh.
- Tối ưu thêm: khớp tên cột **chính xác trước, CHỨA sau** (chịu được hậu tố kiểu `(GMT+8)`); cấp độ cấm khớp bằng **mã trong 【】 qua Set O(1)**; giữ **lần nạp sớm nhất** khi cùng ID; thả tham chiếu workbook sau khi trải AOA; xuất **CSV** thay SpreadsheetML (XML nối chuỗi sẽ sập ở hàng chục nghìn dòng).
- **2 tab nhỏ + chấm điểm lạm dụng (chốt 14/08/2026)**: `NTK.view` = `'ntk'|'abuse'`, nút ở `#ntkViews` (hàng `.tabs-wrap.tabs-inline` + `.tab` — kích cỡ chuẩn dùng chung). `NTK.classify()` chạy sau khi gom nhóm (và mỗi lần đổi ngưỡng) — **rẻ, chỉ duyệt các nhóm đã gom, không đọc lại file**. Điều kiện TÁCH sang **Nhóm Lạm Dụng** (loại trừ với Nhóm Nhiều Tài Khoản): chênh lệch **đúng 0%** (mọi TK nạp y hệt) → **luôn** lạm dụng bất kể số TK (chốt 15/08/2026); HOẶC `số TK > MIN_ACCS` (mặc định **5**, tức từ 6 TK) **VÀ** chênh lệch tiền nạp `(max−min)/max ≤ MAX_SPREAD` (mặc định **20%**). *Trên file thật 16.892 dòng: riêng luật 0% đưa 98/259 nhóm (219 dòng) sang tab Lạm Dụng, phần lớn là nhóm 2 TK.* Cả 2 ngưỡng chỉnh trực tiếp trên giao diện (`#ntkSpread`, `#ntkMinAcc` → `NTK.setThresh()`), không cần deploy lại. Nhóm có TK **thiếu tiền nạp** → `spread=null` → **luôn ở lại tab NTK** (không đủ dữ liệu để kết luận, tránh xử lý oan).
- **Đặc điểm (`g.marks`) — chỉ HIỂN THỊ, không tự tách nhóm**: mẫu ID (`Cùng cấu trúc ID` qua `shapeOf` nén dạng chữ/số · `Cùng tiền tố (N ký tự)` · `Đuôi số liên tiếp` · `Ký tự lộn xộn` qua `messyId` · `Chữ cái lặp bất thường` · `Trùng chuỗi con trong ID` qua 4-gram dùng chung) và chi nhánh (`giống hệt` · `viết thường không dấu` qua `noDiacritic` · `in hoa toàn bộ`). Dùng để xếp nhóm nhiều đặc điểm lên trước khi cùng số TK. ⚠ Nhãn **"Đuôi số thứ tự" đã bị GỠ** vì bắn trên ~70% nhóm (tên người Việt vốn hay kèm số) — làm loãng các nhãn thật; đừng thêm lại. Nhãn `Chi nhánh giống hệt` hiện bắn ~50% (131/259) — còn cao, cân nhắc nếu thấy nhiễu.
- **Tab nhỏ thứ 3 "Check Lạm Dụng" (chốt 15/08/2026)** — diện NGHI NGỜ để nhân viên soi tay, không tự kết luận. **Gom theo IP** (`ipMap` trong `process`, khử trùng ID y như gom tên+IP; chỉ giữ IP có ≥2 TK), khác hẳn 2 tab đầu vốn gom theo **tên+IP** ⇒ bắt được ổ dùng chung IP nhưng **khai tên khác nhau** mà 2 tab kia mù hoàn toàn. `NTK.ipGroups`, key `'ip||'+ip`. Chấm điểm dùng chung `NTK.analyze(g)` (tách ra từ `classify`) nên đặc điểm/chênh lệch tính giống hệt 2 tab đầu.
  - **5 lựa chọn tiêu chí** (`#ntkCrit` → `NTK.CRIT`) + ngưỡng số TK (`#ntkCheckMin` → `NTK.CHECK_MIN`, mặc định 5). ⚠ Ô này là **CHỌN MỘT**, không cộng dồn — user từng tưởng tab thiếu dữ liệu vì chỉ thấy kết quả của 1 tiêu chí. Nên **mặc định là `all` (gộp)** = khớp BẤT KỲ tiêu chí nào (chốt 15/08/2026). Các lựa chọn: `all` · `c` = cùng IP + chênh lệch ≤ `MAX_SPREAD` + ≥1 đặc điểm ID · `c2` = như c nhưng ≥2 đặc điểm ID · `a` = khác tên cùng IP · `b` = khác tên + cùng IP + cùng chi nhánh. `NTK.critHits(g)` trả nhãn tím ghi rõ nhóm lọt vào **vì tiêu chí nào**. *File thật 16.892 dòng, ngưỡng 5: gộp=49, c=10, c2=7, a=49, b=2 (c ⊂ a nên gộp = a); hạ ngưỡng xuống 2 thì c=163, a=808.*
  - `g.dupOfAbuse`: bỏ qua IP-group mà toàn bộ TK nằm gọn trong MỘT nhóm tên+IP đã ở tab Lạm Dụng → không hiện lại việc đã xử lý.
  - Nhóm IP **không có phân loại tự động** — chỉ vào tab Lạm Dụng khi nhân viên bấm `⇢ Chuyển nhóm LD` (`NTK.manual['ip||…']=true`); ở tab Lạm Dụng nút đổi thành `↩ Trả về Check`. Tab Lạm Dụng render **cả hai loại nhóm**; `filtered()` nối `groups.filter(abuse)` + `ipGroups.filter(abuse)`.
  - **Kết luận GỢI Ý (chốt 15/08/2026)** — `NTK.verdict(g)`: điểm = tiền y hệt `+3` (hoặc trong ngưỡng `+2`) · mỗi đặc điểm ID `+1` (tối đa 3) · chi nhánh giống hệt `+1` · số TK ≥ 2×`CHECK_MIN` `+1`. Nhãn: `≥6` **Nên đưa vào Lạm Dụng** (đỏ) · `4–5` **Nghi ngờ cao** (cam) · `≤3` **Cần xem thêm** (xám). Chỉ là GỢI Ý — nhóm IP vẫn KHÔNG tự sang tab Lạm Dụng, nhân viên phải bấm. Tab Check sắp xếp theo **điểm giảm dần** (2 tab kia vẫn theo số TK). Nhãn đếm hiện thêm `· N nhóm nên chuyển` (`st.checkSuggest`). *File thật, tiêu chí c ngưỡng 5: 10 nhóm, điểm 4–6, 2 nhóm được khuyên chuyển.*
  - Xuất file: tab Check dùng layout 9 cột như tab NTK, tên file `Check_Lam_Dung_*.xls`.
- **Chuyển tay giữa 2 tab (chốt 14/08/2026)**: nút trong ô Đặc điểm mỗi nhóm — `⇢ Chuyển nhóm LD` (tab NTK) / `↩ Trả về Nhiều TK` (tab LD) → `NTK.move(key,toAbuse)`. Ghi đè lưu ở `NTK.manual{key→bool}` với `key = chuẩn_hóa(tên)+'||'+IP` (gán lúc dựng nhóm), nên **giữ nguyên khi đổi ngưỡng** (classify chạy lại vẫn áp ghi đè). Trùng với kết quả tự động thì tự xóa khỏi `manual` cho gọn; nhóm bị chuyển tay có nhãn xanh **"Chuyển tay"** (`g.moved`). `manual` bị reset khi nạp file mới và khi Xóa dữ liệu.
- **Phân trang theo SỐ DÒNG (không phải số nhóm)**: ô chọn `#ntkRows` — 200/500/1000/2000/Tất cả (`NTK.ROWS`, 0 = tất cả) → `NTK.pageSlices()` gom nhóm cho tới khi chạm giới hạn, **không xé lẻ một nhóm** (nhóm vượt quá bị đẩy sang trang sau, nên số dòng mỗi trang có thể xấp xỉ chứ không khít). Nhãn đếm hiện cả `N nhóm · M dòng`.
- **Bảng**: căn giữa toàn bộ (`.ntk-tbl th,td{text-align:center}`), cột đổi tên `Nạp Trước`/`Nạp Sau`, thêm 2 cột gộp ô dọc `Chênh lệch` + `Đặc điểm` (rowspan theo số TK), có dòng đệm `.ntk-gap` giữa các nhóm. **Đã bỏ nút Copy câu lệnh và dòng câu lệnh khỏi bảng** (theo yêu cầu) — câu lệnh CHỈ còn trong file xuất ra.
- **Xuất file = EXCEL (SpreadsheetML .xls), KHÔNG phải CSV (chốt 15/08/2026)**: user cần căn giữa + độ rộng cột vừa chữ, mà CSV là văn bản thuần nên **không mang được định dạng** — bắt buộc phải là Excel. `NTK.exportCsv()` (giữ tên hàm) nay sinh SpreadsheetML giống cách `BC.genExcel` làm (BOM + `<?xml encoding="UTF-8"?>` + `<?mso-application?>`, Tahoma 9, viền mảnh, tiêu đề nền `#F8CBAD`). Style `c` = căn giữa, `l` = căn trái. Độ rộng mỗi cột tính theo **ô dài nhất của chính cột đó** (`len*5.6+16`, chặn 55–260pt, riêng cột câu lệnh 620pt). Cột **Tiền nạp** xuất kiểu `Number` để Excel cộng được; các cột còn lại ép `String` để không mất số 0 đầu. Cảnh báo confirm nếu > 60.000 dòng.
  - **Tab Nhiều Tài Khoản** — 9 cột: `Nạp Trước · Nạp Sau · Cấp độ thành viên · Tên thật · Tiền nạp · Chi nhánh · IP · Tài khoản · Câu lệnh NTK`. Căn giữa tất cả, RIÊNG **Câu lệnh NTK căn trái**. ⚠ 3 cột `Thời gian nạp / Chênh lệch / Đặc điểm` **CỐ Ý không xuất** (user không cần) — chúng chỉ tồn tại trên giao diện.
  - **Tab Lạm Dụng** (đổi 15/08/2026 theo ảnh mẫu user) — 7 cột: `Tài khoản · Cấp độ thành viên · Tên thật · Tiền nạp · Chi nhánh · IP · Câu lệnh NTK`. Mọi TK nạp trước/sau đổ chung cột `Tài khoản`, **KHÔNG có dấu phẩy đuôi** (bản 2 cột cũ có, nay bỏ). Cột cuối lặp hằng `NTK.ABUSE_CMD` cho mọi dòng. **Chèn 1 dòng TRỐNG giữa các nhóm** (`body.push(null)` → `<Row/>`).
  - **Tab Check KHÔNG xuất file**: `#ntkExportBtn` bị ẩn khi `view==='check'`, kèm chốt chặn trong `exportCsv` (alert hướng dẫn chuyển nhóm sang tab Lạm Dụng rồi tải). Tab này là diện nghi ngờ chưa kết luận nên cố ý không cho xuất.
- ⚠ **Câu lệnh NTK gắn `data-noi18n`** — là nội dung nghiệp vụ dán sang hệ thống khác, dịch sang tiếng Anh là hỏng (cùng nhóm với tên nhân viên/đại lý).
- ⚠ Handler dán của **Đơn Rút** (`nav-donrut.js`) nay kiểm thêm `t1` phải đang hiện, nếu không tab NTK sẽ bị nó cướp sự kiện dán.
- **Đã đo (máy dev, 14/08/2026)**: file `.xlsx` thật 200.000 dòng / 59 MB → `XLSX.read` **2,6 s** (đồng bộ, màn hình đứng — đã ghi rõ trong thanh tiến trình), lọc+gom **0,8 s**, render **9 ms**, xuất CSV 37 MB **0,35 s**. Kết luận: **chưa cần Web Worker**; nếu máy nhân viên yếu tới mức khó chịu thì bước tiếp theo là đẩy `XLSX.read` sang worker (blob URL, không cần build step).

### T4 — QUY TRÌNH LÀM VIỆC (SOP, chốt 18/08/2026 · đổi tên + mở quyền xem 19/08/2026)
- Tab lớn thứ 4 (`#t4`, nút `#tsb4` — nhãn **QUY TRÌNH LÀM VIỆC**), **LƯU CLOUD** (khác NTK/DR session-only). File `src/js/sop.js` (module `SOP`), CSS `.sop-*` cuối `styles.css`, HTML khung trong src html (JS render toàn bộ).
- **Quyền (chốt 19/08/2026)**: **MỌI tài khoản đăng nhập XEM được**; **ADMIN + Tổ Trưởng** được sửa. `applyPerms` luôn hiện `#tsb4`, ẩn/hiện cụm `#sopAdminTools` (Chế độ biên tập · Xuất nội dung · Nhập bản dịch · ô Giữ nguyên ảnh gốc) theo `isTTup`; `SOP.canEdit()` (= admin || totruong) chặn mọi thao tác ghi kể cả khi ép `SOP.edit=true`. Backend: `reports_select` (supabase_update4.sql) cho authenticated đọc; **`supabase_sop_setup.sql` mục 2b** mở policy đọc ảnh cho authenticated; **`supabase_sop_totruong.sql`** thêm `'sop_index','sop_item'` vào `can_write_report()` cho Tổ Trưởng + đổi 3 policy ghi/sửa/xoá ảnh sang `my_role() in ('admin','totruong')`. User tự chạy cả hai.
- **4 tab nhỏ** (`SOP.TABS`, kiểu `.tab` gạch chân như T1, KHÔNG icon): `qt` Quy Trình Duyệt Đơn · `ld` Nhóm Lạm Dụng · `game` Trò Chơi Lạm Dụng · `doc` Lưu Trình Công Việc. Cả 4 dùng CHUNG bố cục 2 cột: cột trái `#sopRail` (mục gom theo **nhóm**, chấm màu mức độ, ô tìm), cột phải `#sopPane`.
- **3 tầng dữ liệu**: nhóm → mục → (bước/trò chơi) → khối. `qt` dùng `subs` kiểu **step** (thanh bước ngang 1─2─3 + nút `+`); `game` dùng `subs` kiểu **acc** (mỗi trò chơi 1 khối gập, có thẻ nhận diện: ảnh bìa + mã game + trạng thái + áp dụng từ); `ld`/`doc` không có subs, khối gắn thẳng vào mục.
- **Khối nội dung** (`blocksHtml`/`blockHtml`, thêm/xoá/↑↓ tự do, xen kẽ tuỳ ý): `text` (tiêu đề nhỏ + nhiều đoạn) · `img` (nhiều ảnh, bố cục 1/2/3 cột, chú thích riêng) · `feat` (danh sách đánh số) · `table` (chỉ `doc`) · `info`/`warn`/`ban` (khung xanh/vàng/đỏ). **KHÔNG có nhãn loại khối** ở đầu (đã gỡ hết "VĂN BẢN/HÌNH ẢNH/BẢNG" theo yêu cầu 18/08); khối `feat` chỉ hiện tiêu đề khi user tự đặt `b.h`.
- **Soạn TẠI CHỖ, KHÔNG dùng `prompt`/`confirm`/`alert` của trình duyệt** (chốt 18/08/2026):
  - `SOP._ed={scope,i,k}` + `edOpen/edSave/edCancel/edBar` → khối `text`/`info`/`warn`/`ban`, mục `feat`, chú thích ảnh đều biến thành ô nhập ngay tại dòng (input tiêu đề `#sopEdH` + textarea `#sopEdB`) kèm nút Lưu/Huỷ. Thêm khối mới = tạo rỗng rồi mở ô soạn luôn; Huỷ khi còn rỗng thì **tự gỡ khối**. Phím tắt Ctrl+Enter lưu · Esc huỷ (bắt ở **capture**, nếu không handler đóng `.dr-modal` chung của DR nuốt mất).
  - Bảng: sửa **thẳng trên ô** (`contenteditable` + `tblCell`), nút ＋/－ Dòng/Cột. **Rộng cột · căn chữ · màu ô (chốt 22/08/2026)**: khối bảng có thêm `b.w` (mảng % mỗi cột, tổng luôn 100) · `b.al` (`'l'|'c'|'r'` mỗi cột, mặc định `c`) · `b.bg` (`{"ri|ci":'go'|'re'|'gr'|'bl'|'pu'}`). Render tách ra `tableHtml()` sinh `<colgroup>` + class `al-*`/`bg-*`; CSS `table-layout:fixed` (không có thì % cột bị bỏ qua) + `overflow-wrap:anywhere`. Kéo viền: `.sop-cres` ở `<th>` (trừ cột cuối) → `tblResize` lấy/nhả với **cột kế bên** nên tổng giữ 100%, lưu lúc `mouseup`. `tblAlign` hỏi cột rồi hỏi kiểu căn qua `SOP.dlg`. Tô màu: `tblPaintMode` bật cờ `SOP._paint={scope,i}` — khi bật thì ô **bỏ `contenteditable`** (không con trỏ nhảy vào) và bấm ô mở bảng màu `tblPaintCell`. `tblCol` co giãn `w`/`al` và dọn `b.bg` của cột bị xoá; `tblRow` dọn `b.bg` của dòng bị xoá. **Gộp ô (chốt 22/08/2026)**: `b.mg={"ri|ci":[rowspan,colspan]}`; `tblSkip()` dựng danh sách ô bị nuốt (KHÔNG render, chữ vẫn nằm trong `b.rows` nên bỏ gộp là hiện lại). Chế độ gộp `SOP._mg={scope,i,r,c}` (loại trừ với `_paint`): bấm ô đầu (highlight `.sel`) rồi ô cuối → gộp vùng chữ nhật; bấm lại ô gộp → hỏi *Bỏ gộp / chọn làm ô đầu*. **Chặn vùng chọn cắt ngang một ô gộp sẵn** (báo lỗi thay vì để bảng vỡ khung). Xoá dòng/cột: ô gộp chạm vùng bị xoá thì co `rowspan`/`colspan`, còn 1×1 thì bỏ khỏi `b.mg`. ⚠ `tblCell` đọc `el.textContent` **KHÔNG** `innerText` — hàng tiêu đề có `text-transform:uppercase`, `innerText` trả chữ ĐÃ in hoa và ghi đè hỏng dữ liệu gốc.
  - **Ô bảng SONG NGỮ (chốt 24/08/2026)**: bản VI ở `b.rows`, bản EN ở **`b.en_rows`** (lưới cùng hình dạng), `b.envi_rows` = dấu vết bản VI lúc dịch (stale) — tương đương `o.en`/`o.envi` của mọi khối khác. Helper cạnh `tblCols`: `tblGrid(b,f)` (lấy/tạo + tự nới lưới phụ theo `b.rows`) · `tblTrim(b)` (cắt lưới phụ sau khi xoá dòng/cột — gọi trong `tblRow`/`tblCol` nhánh xoá) · `tblTx(b,ri,ci)` (EN khi đang bật EN và ô có bản dịch, thiếu thì **rơi về VI** — nhờ vậy cột ghi chú không dịch vẫn hiện tiếng Việt ở bản EN). `tableHtml` render qua `tblTx` chứ không in thẳng ô. `tblCell` khi đang bật EN thì ghi vào `en_rows` (sửa cái đang thấy), KHÔNG đè bản VI. `paths()` xuất `en`/`cu` lấy từ `en_rows`/`envi_rows`. ⚠ **Bug đã sửa**: `setPath` cũ có nhánh `if(Array.isArray(o)){o[ci]=en}` — nhập bản dịch **ghi tiếng Anh ĐÈ THẲNG lên `b.rows`**, mất hẳn tiếng Việt và bảng kẹt 1 ngôn ngữ (đã xảy ra thật với 3 bảng tab `doc` ngày 24/08/2026). Nay `setPath` bắt đường dẫn `^(.*)\.rows\.(\d+)\.(\d+)$` TRƯỚC vòng walk và chỉ ghi `en_rows`; kèm **cơ chế tự vá**: nếu ô VI đang chứa ĐÚNG chuỗi EN sắp ghi thì trả lại `vi` trong file dịch (khớp tuyệt đối nên không đụng ô người soạn sửa tay sau khi xuất file) ⇒ nhập lại đúng file dịch cũ là dữ liệu tự hồi phục.
  - Hộp thoại chung `SOP.dlg()` (`#sopDlg`, class `.dr-modal` nên tự canh giữa màn hình): 3 kiểu — xác nhận (nút đỏ) · nhập chữ · chọn (danh sách nút, dùng cho mức độ + chọn nhóm). `confirmBox/promptBox/askLevel` gọi qua nó. ⚠ Esc/bấm nền phải gọi `dlgClose(null)` để **trả Promise**, không thì lời gọi `await` treo mãi.
- **Liên kết chéo giữa các bài (chốt 20/08/2026)**: nội dung nhúng cú pháp `[[<id mục>|<chữ hiển thị>]]` (id dạng `i_xxxx` do `SOP.uid('i')` sinh). `SOP.rich(s)` = `hesc` + đổi cú pháp thành `<a class="sop-lnk">` — dùng thay `hesc` ở `para()` (khối text), khung info/warn/ban, và `feat` (`b`/`d`). Bấm → `SOP.goto(id)`: `findItemAny` → tự **đổi tab nhỏ** nếu bài ở tab khác (`renderTabs`) rồi `open(id)` + hiệu ứng `.sop-jump`. Bài đã xoá → `<span class="sop-lnk dead">` (không phải link chết) + `goto` báo hộp thoại. Người soạn KHÔNG gõ tay: nút **🔗 Liên kết bài** ở `edBar()` → `SOP.insertLink()` (chọn bài qua `SOP.dlg` options gộp cả 4 tab → nhập chữ hiển thị → chèn tại con trỏ; bôi đen sẵn thì lấy làm chữ mặc định). `edBar(1)` = ẩn nút này (chỉ ô chú thích ảnh). ⚠ Vị trí con trỏ được chụp TRƯỚC khi `await` hộp thoại (focus rơi sang `#sopDlgI`). ⚠ Khi dịch EN phải **giữ nguyên khối `[[...]]`** trong chuỗi (chỉ dịch phần chữ sau `|`). CSS `.sop-lnk/.sop-lnk.dead/@keyframes sopJump` cạnh `.sop-featb`.
- **Thẻ trò chơi (tab `game`) — chốt 22/08/2026**: `gameCardHtml` đổi nhãn `Mã game` → **`Tên game`** (field dữ liệu VẪN là `s.code`, đừng đổi tên field kẻo vỡ bản ghi cũ); giá trị **Trạng thái + Áp dụng từ tô vàng** `--go` (class `.sop-grow b.go`) và **bỏ `data-noi18n`** ở 2 dòng này để `Hạn chế/Cấm cược/Theo dõi` dịch được — riêng dòng Tên game giữ `data-noi18n` (chữ người dùng gõ). Ảnh bìa ép **khung VUÔNG** `aspect-ratio:1/1;object-fit:contain` (chỉ để logo, contain để không cắt chữ trong logo). Thêm **Ghi chú** `s.note` (`gameNoteHtml`, khung `.sop-gnote` dưới ô thông tin, lấp khoảng trống bên phải): soạn TẠI CHỖ bằng cờ riêng `SOP._gnEd` (không phải khối nên không dùng được `SOP._ed`/`blocksOf`), textarea id `sopGnB` + `SOP.gnKey` cho Ctrl+Enter/Esc, `editGameNote/saveGameNote/cancelGameNote`. Ghi chú qua `SOP.rich()` nên nhúng được `[[...]]`, và có trong `paths()` (`subs.<i>.note`) nên xuất/nhập bản dịch thấy.
- **Dấu "MỚI" (chốt 23/08/2026)**: đánh dấu thông tin mới, hiện **cùng lúc ở khối nội dung VÀ ở cột chủ đề bên trái**. 2 field: `it.new` (người soạn tích thẳng vào mục, lưu ở `sop_index`) và `it.hasNew` (SUY RA, `syncCount()` đặt `= subs.some(s=>s.new)` khi tab có subs). `s.new` = dấu ở từng bước/trò chơi (lưu trong `sop_item`). Hiển thị: `SOP.isNew(it)` (new||hasNew) → chip ở dòng `.sop-it` cột trái · `it.new` → chip cạnh tên mục `.sop-t` · `s.new` → chip trên thanh `.sop-acch` (trước chip mức độ) và ở tiêu đề `.sop-subt` của bước. Chip = hằng `SOP.NEWCHIP` — chữ **"New" GIỮ NGUYÊN ở cả VI lẫn EN** (gắn `data-noi18n`, đừng thêm khoá "Mới" vào `I18N.EN`) + **6 thẻ `<i>` = 6 ngôi sao lấp lánh** quanh viền, nháy lệch nhịp .19s, chu kỳ 1,8s (mật độ "cấp 2 — vừa", user chọn từ 5 mẫu mô phỏng 23/08/2026). Vị trí/kích thước sao đặt bằng `.sop-new i:nth-child(1..6)` trong CSS; sao nằm NGOÀI viền chip nên **đừng đặt `overflow:hidden`** cho chip hay dòng chứa nó; dòng đang chọn đảo màu nên có thêm `.sop-it.on .sop-new i{background:var(--pu)}` (sao trắng trên nền trắng sẽ tàng hình). Có `@media (prefers-reduced-motion:reduce)` tắt nháy. Bật/tắt (chỉ khi bật Chế độ biên tập): **CHÍNH CHIP LÀ NÚT** — `SOP.newChip(on,act)` (chốt 23/08/2026, đã GỠ nút "Đánh dấu mới" ở đầu bài và nút 🆕 trong `.sop-accb`): `act` rỗng = chỉ hiển thị (người xem + cột chủ đề, chưa bật thì không hiện gì); `act` có JS = luôn hiện, chưa bật thì **mờ `.sop-new.off`** (nền xám, ẩn sao), bấm là bật kèm hiệu ứng ngay. Chip ở tên bài gọi `SOP.toggleNewItem()` (ghi `sop_index`), chip trên thanh trò chơi/tiêu đề bước gọi `SOP.toggleNewSub(i)` (ghi `sop_item` rồi `syncCount()` cập nhật `hasNew` cho cột trái). ⚠ Chip nằm trên `.sop-acch` (bấm thanh = gập/mở) nên onclick phải `event.stopPropagation()` — `goAcc` chỉ bỏ qua click vào `<button>`, chip là `<span>`. CSS `.sop-new` cạnh `.sop-lvl` (gradient tím-xanh; riêng dòng đang chọn `.sop-it.on .sop-new` đảo thành nền trắng chữ tím). KHÔNG tự hết hạn — người soạn tự bỏ dấu.
- **Thanh trò chơi + mở nhiều cùng lúc (chốt 22/08/2026)**: trạng thái mở nằm ở **`SOP.accOpen{chỉ số→true}`**, KHÔNG dùng `SOP.curSub` nữa (curSub chỉ còn cho thanh bước tab `qt`) ⇒ bung trò này không đóng trò kia. `goAcc` toggle 1 mục · `accAll(1|0)` mở/đóng tất cả · `open(id)` reset `{}` = **ĐÓNG HẾT** (chốt 23/08/2026, trước đây mở sẵn trò đầu `{0:true}` — user muốn tự mở trò cần xem) · `moveSub` hoán vị cờ theo trò chơi · `delSub` **dồn chỉ số** (không thì mở nhầm trò khác) · `addSub` mở sẵn trò vừa thêm. Thanh header (kiểu 2 user chọn): số thứ tự tròn nền **galaxy** `.sop-accn` · logo 34px `.sop-accth` (dùng `img[data-p]` nên `hydrateImgs` tự ký URL) · tên + dòng phụ trong `.sop-acci` · chip mức độ **tô nền đặc** `.sop-lvl.solid` (chip mờ chìm trên nền xanh) · nền thanh **xanh nhạt** `rgba(59,130,246,.09)`, viền TRÁI 4px theo mức độ (`.lv-hi/.lv-go/.lv-mu`). ⚠ `.sop-acc.open` phải đổi **3 cạnh riêng lẻ** (`border-top/right/bottom-color`) — dùng `border-color` là mất luôn viền trái màu mức độ. Hàng `.sop-accbar` có Mở/Đóng tất cả + bộ đếm "N trò chơi · M đang mở" (dịch bằng `I18N.RX`).
- **Bố trí nút sửa** (chốt 18/08): `.sop-btools` là **một hàng riêng ở đầu khối** (mờ .45, rõ khi rê chuột) — trước để `position:absolute` góc phải nên che mất phần trên ảnh chụp màn hình. Nút của từng ảnh `.sop-imgb` nằm **dưới dòng chú thích**, không nổi trên ảnh.
- **Thanh bước (tab `qt`)** — kiểu "ray liền" (chốt 19/08/2026): khung nền ĐẶC `--sop-bar:#121c39` + viền xanh; mỗi chấm có `outline:5px solid var(--sop-bar)` để **cắt ray khi đi qua chấm** (đổi nền khung phải đổi biến này theo). Ray `.sop-srail::before/::after` chạy từ **tâm chấm đầu → tâm chấm cuối**, phần sáng `width:calc((100% - 104px) * var(--p))` với `--p = curSub/(n-1)` ⇒ mút phải trùng đúng tâm chấm hiện tại ở mọi số bước. ⚠ Tên class là **`sop-srail`** — `.sop-rail` ĐÃ là cột chủ đề bên trái, trùng tên sẽ vẽ ray vào sidebar; `.sop-srail{flex:none}` (để co giãn thì ray bị bóp hẹp hơn hàng chấm → phần sáng sai vị trí); khung `overflow-x:auto` chứ không cho xuống dòng (xuống dòng làm đứt ray). Trạng thái: `todo` xám mờ · `read` = **các bước TRƯỚC bước đang xem**, tô gradient y hệt bước đang xem · `on` cùng gradient + quầng sáng. **KHÔNG nhớ "bước đã từng mở"** (đã gỡ `readMax`/localStorage `sop_read`: đang ở bước 1 mà bước 2 vẫn sáng là sai). Tên bước luôn `--tx` weight 400 (không sáng theo trạng thái).
- **Chữ & màu** (chốt 19/08/2026): mọi cấp tiêu đề dùng CHUNG form chữ `font-weight:700 · letter-spacing:.05em · text-transform:uppercase` và màu **`--bl2` #60a5fa** — `.sop-grp` (tên nhóm, .66rem) · `.sop-t` (tên mục, 1.05rem) · `.sop-subt` (Bước N, .86rem) · `.sop-bh` (tiêu đề khối, .9rem) · `.sop-acct` (tên trò chơi) · `.sop-eh` (ô nhập tiêu đề, để gõ sao thấy vậy). Thân bài `.sop-body p`: .82rem, giãn dòng 1.95, cách đoạn 10px, `max-width:80ch`. Tab nhỏ `#sopTabs .tab` dùng **nguyên kích cỡ `.tab` chuẩn** (phần đè `.75rem` / không in hoa đã gỡ 28/08/2026 — xem "QUY ƯỚC TAB NHỎ"). Dòng mục cột trái `.sop-it` = **nút chuẩn**: thường trong suốt viền mảnh, đang chọn gradient tím-xanh như `.abtn-pu`.
- **Lưu**: `reports` type **`sop_index`** month `'all'` = mục lục 4 tab; type **`sop_item`** month = **id mục** = nội dung 1 mục (tách nhỏ để mở mục nào tải mục đó). Tự lưu sau mỗi thao tác (`saveItem`/`saveIndex` + chip "✓ Đã lưu"), KHÔNG có nút Lưu.
- **Ảnh**: Storage bucket **`quytrinh` (private)**. Nén `compress()`: tối đa **1920px, WebP 0.85** (~120–250 KB/ảnh chụp màn hình, chữ vẫn sắc); ô `#sopNoCompress` bỏ qua nén cho ảnh cần soi. Hiển thị bằng **signed URL 1h** cache RAM (`SOP._urls`, `hydrateImgs` điền `img[data-p]` sau render). Bấm ảnh mở `#sopLb` (dùng `.dr-modal` chung) phóng to tới 400% + mở ảnh gốc tab mới. Xoá ảnh xoá luôn file Storage.
- **Song ngữ (mở rộng 20/08/2026)**: nhãn giao diện có trong `I18N.EN`; nội dung người dùng gõ gắn `data-noi18n`. Mỗi chuỗi có chỗ chứa `o.en[field]` + dấu vết `o.envi[field]`; `SOP.tx()` chọn EN khi `I18N.lang==='en'` (helper `SOP.en()`), thiếu thì rơi về VI — **tên bước / tên trò chơi cũng đi qua `tx()`**, tên mục qua `nameOf()`, tên nhóm qua `grpName()`.
  - **Xuất nội dung** (`exportTrans`): JSON mọi chuỗi kèm đường dẫn + cờ `stale`; **bản ghi đầu `id:'__index'`** chứa tên NHÓM (`p:'g.<gid>'`) vì tên nhóm nằm ở `sop_index` chứ không ở nội dung mục. **Nhập bản dịch** (`onTransFile`): `__index` → ghi `g.en_name`; các mục → `setPath` ghi `en`, ô bảng ghi đè thẳng `rows[ri][ci]`; tên mục chép thêm `en_name` sang mục lục.
  - Chuỗi do JS ghép có SỐ (`1 bước · cập nhật …`, `Bước 1 —`, `2 ảnh`, `3 game`) dịch bằng **`I18N.RX`** — ⚠ `trs()` **trim** chuỗi trước khi khớp nên regex KHÔNG được có khoảng trắng ở hai đầu (đã vấp: `/^Bước (\d+) — $/` không bao giờ khớp).
  - Phép thử: bật EN rồi quét text node + `placeholder/title` trong `#t4` (bỏ vùng `data-noi18n`) → 0 chữ Việt sót, kể cả khi đang mở ô soạn và hộp thoại.
- **Ảnh riêng cho bản EN (chốt 20/08/2026)**: mỗi ảnh có thêm `im.en_u`, ảnh bìa trò chơi có `s.en_thumb`. `SOP.imgSrc(im)` trả `en_u` khi đang bật EN, thiếu thì dùng ảnh VI. Nút mỗi ảnh: **＋ Ảnh bản EN / Đổi ảnh EN / Xoá ảnh EN**; khi EN đang hiện ảnh EN thì có dải `.sop-enflag` "Đang xem: ảnh bản EN". Upload đi qua `SOP._imgTarget={scope,i,thumb,en,enThumb}` — nhánh `en>=0` và `enThumb>=0` chỉ nhận 1 file và **xoá file EN cũ trên Storage**; `delImg` xoá cả `u` lẫn `en_u`. Bản dịch KHÔNG cần khai đường dẫn ảnh (không nằm trong file JSON).
- `window.SOP=SOP` cuối file (cùng bẫy `const` global-lexical như `AUTH` — i18n.js kiểm `window.SOP`).
- Nút **In** (chỉ tab `doc`): `printDoc()` gắn `body.sop-printing`, CSS `@media print` ẩn chrome, nền trắng chữ đen.
- SQL: **`supabase_sop_setup.sql`** (tạo bucket + 4 policy ADMIN; kèm hướng dẫn mở cho nhân viên xem sau này). User ĐÃ chạy 19/08/2026 — bucket `quytrinh` tồn tại với 4 policy.
- ⚠ **Đừng dùng `sed -i` kiểu `'/^$/{/./!d}'` trên `styles.css`** — đã lỡ xoá sạch dòng trống của cả file (phải `git checkout <sha> -- src/styles.css` để lấy lại). Sửa CSS thì dùng Edit theo chuỗi chính xác.

### T5 — KIỂM TRA NGHIỆP VỤ (bài test nhân viên, chốt 26/08/2026)
- Tab lớn thứ 5 (`#t5`, nút `#tsb5`). File `src/js/exam.js` (module `EX`, `window.EX=EX`), CSS `.ex-*` cuối `styles.css`, HTML khung trong src html (JS render toàn bộ vào `#exBody`).
- **Lưu THẲNG trên Supabase** (chốt 26/08/2026 — bản đầu để ở Google Sheet + proxy qua Edge Function, đã **gỡ bỏ hẳn**). Lý do đổi: tính ra con số thì nỗi lo dung lượng không có cơ sở — 20 nhân viên × 20 câu × 1 lần/tháng ≈ **6 MB/năm kể cả index**, tức ~1,2% của 500 MB gói Free; đổi lại bỏ được cả Apps Script, proxy, token và cái quy trình "Deploy bản mới" gây kẹt. SQL: **`supabase_exam_setup.sql`** (user tự chạy).
- ⚠ **BẢO MẬT NẰM Ở TẦNG POSTGRES, KHÔNG Ở JS** (repo public + anon key): nhân viên **KHÔNG có quyền SELECT** trên `exam_questions` / `exam_submissions` / `exam_answers` (3 bảng chứa đáp án mẫu). Họ chỉ gọi được RPC `exam_*` (SECURITY DEFINER, tự kiểm `my_role()` bên trong). Đề do `exam_start()` bốc ngẫu nhiên **trong SQL** rồi trả về bản đã cắt `answer` ⇒ không tự chọn được câu dễ, không đọc trộm được đáp án, không sửa được điểm/lượt. Ẩn nút trong `EX.renderViews()` chỉ để cho gọn mắt.
- **RPC**: `exam_boot` (chủ đề+cấu trúc+cài đặt+lượt của tôi+bài dở) · `exam_start` · `exam_submit` · `exam_my_list` (đáp án mẫu kèm hay không do cài đặt `show_answer`) · `exam_grade` · `exam_list` · `exam_members_list` · `exam_rank`. TT/admin thì đọc ghi **thẳng bảng** cho 3 tab quản lý (RLS `my_role() in ('admin','totruong')`).
- **Khóa dữ liệu theo `user_id` (UUID Supabase)**, KHÔNG theo tên: đổi tên tài khoản vẫn giữ nguyên lịch sử test (bài học CHAMY→SOLIS). Danh sách nhân viên **không gõ tay** — `exam_members_list()` join thẳng bảng `profiles`.
- **Quyền**: MỌI tài khoản đăng nhập đều LÀM BÀI + xem bài của mình (`applyPerms` luôn hiện `#tsb5`); ADMIN + Tổ Trưởng thêm 3 tab nhỏ Quản Lý Đề / Quản Lý Nhân Viên / Chấm Điểm (`EX.canEdit()` lọc `EX.VIEWS`).
- **5 tab nhỏ** (`.tabs-wrap.tabs-inline` `#exViews`, class `.tab`): `lam` Làm Bài · `bai` Bài Của Tôi · `de` Quản Lý Đề · `nv` Quản Lý Nhân Viên · `cham` Chấm Điểm. Xếp hạng KHÔNG ở đây — nằm ở tab Xếp Hạng của T1 (xem mục trên).
- **Bố cục**: màn quản lý dùng `.ex-2col` (rail 300px + pane, sticky); màn LÀM BÀI **cố ý giữ cột đọc hẹp** `.ex-narrow`/`.ex-qcard` max 900px (dòng dài quá ~85 ký tự rất khó đọc) + thanh trạng thái `.ex-bar` tràn hết bề ngang + lưới số câu `.ex-qnav` bên phải để nhảy câu.
- **Quản Lý Đề — bố cục "B" (chốt 28/08/2026)**: rail rộng `.ex-2col-wide` (340px) và mỗi chủ đề là `.ex-trow2` chiếm **2 dòng** — dòng trên TÊN ĐẦY ĐỦ (`white-space:normal`, ghi đè `.ex-tname` vốn nowrap+ellipsis), dòng dưới ô số câu + `/ N câu có sẵn` + 2 nút. Bản đầu nhồi cả 6 thứ vào 1 dòng 300px nên tên chủ đề bị cắt cụt ("Sảnh — Ba…", "Tự luận ng…"), user chê. Đã dựng 3 phương án mô phỏng cho user chọn (A dải ngang · **B** ✔ · C chip lọc + ngân hàng toàn màn); nếu sau này thấy chật nữa thì C là hướng tiếp theo.
- **Lọc ngân hàng theo chủ đề**: ô `<select class="ex-inp ex-filter">` ở tiêu đề "Ngân hàng câu hỏi" (kèm số câu mỗi chủ đề) → `EX.pickTopic()`, **dùng chung biến `selTopic`** với cột trái nên bấm bên nào cũng ra cùng kết quả. CSS `.sec-hdr .ex-filter{order:1}` để nằm sau đường kẻ `::after` (sát mép phải) — cùng mẹo với `.sec-hdr .lang-sw`.
- **ẢNH CÂU HỎI — tải thẳng, KHÔNG dán link (chốt 28/08/2026)**: cơ chế y hệt T4, bucket **`baitest`** (PRIVATE, SQL `supabase_exam_storage.sql`). `EX.compress` (1920px, WebP .85) → `EX.uploadImg` → `exam_questions.image_url` lưu **đường dẫn** trong bucket; hiển thị bằng signed URL 1h cache RAM (`EX._urls`, `EX.hydrateImgs()` điền `#t5 img[data-p]` SAU mỗi lần render — các hàm render async phải tự gọi lại). `EX.imgHtml` tự phân biệt path và `http(s)` nên **giá trị cũ dạng link vẫn chạy** và ô dán hàng loạt vẫn nhận cột link ảnh. Nhập ảnh: kéo thả · bấm chọn · **dán Ctrl+V** (handler `paste` cuối `exam.js`, chỉ ăn khi T5 đang hiện + view `de` + clipboard có FILE ảnh ⇒ không cướp dán của Đơn Rút/NTK, không cản dán chữ). Bấm ảnh mở `EX.lightbox`.
  - ⚠ **KHÔNG xóa ảnh của câu hỏi ĐÃ lưu** (chốt 28/08/2026, sửa lỗi thật): `exam_answers.image_url` là bản chụp lúc bốc đề, nên sửa/xóa câu hỏi mà dọn file thì **mọi bài đã nộp trước đó hiện "Không tải được ảnh"** — đúng lỗi user gặp ở tab Chấm Điểm và Bài Của Tôi. `EX.dropImg` giờ chỉ được gọi khi `!this.editQ` (ảnh vừa tải nhầm của câu hỏi CHƯA lưu); `delQ` không đụng ảnh nữa. Ảnh mồ côi ~200 KB/cái, rẻ hơn nhiều so với vỡ lịch sử chấm điểm. Nhân viên xem được ảnh lúc làm bài và tra cứu vì policy `baitest_read` cho mọi `authenticated` đọc (ảnh không phải bí mật — đáp án mẫu mới là, và nằm ở cột `answer` họ không đọc được). **1 ảnh/câu hỏi** — cần nhiều ảnh thì phải đổi schema.
- **Đồng hồ**: `exam_start`/`exam_boot` trả `elapsed` (giây, server tính) chứ KHÔNG để client parse chuỗi thời gian — chuỗi không mang múi giờ, máy nhân viên lệch múi là đồng hồ sai hoặc **báo hết giờ ngay** (đã vấp lúc thử ở bản Apps Script). Client giữ `run.t0 = Date.now() - elapsed*1000`.
- **Lượt test**: trừ NGAY khi bắt đầu (chống bấm lại để đổi đề), nhưng đề lưu ở `exam_submissions.questions` với `status='doing'` ⇒ F5 / mất mạng / đóng tab vào lại **nhận đúng đề cũ, KHÔNG trừ thêm lượt**. Bỏ quá `duration+30` phút thì chuyển `status='abandoned'`. Câu trả lời còn có **nháp `localStorage`** (`ex_draft_<mã bài>`).
- **Bảng**: `exam_topics · exam_questions · exam_config · exam_settings · exam_members · exam_submissions · exam_answers`. `exam_answers` ~400 dòng/tháng. Xếp hạng tính bằng SQL (`distinct on (user_id)` lấy **bài tốt nhất trong kỳ**) — không cần cache, cột `month` (GMT+7) có index.
- **Dán hàng loạt từ Excel** (`EX.openPaste/parsePaste/runPaste`): giữ lại cái tiện duy nhất của bảng tính. Mỗi dòng = `Chủ đề ⇥ Độ khó ⇥ Câu hỏi ⇥ Đáp án ⇥ Link ảnh`; chủ đề chưa có được tạo mới tự động, link Google Drive tự đổi sang link ảnh (`EX.normImg`), dòng thiếu cột bị bỏ qua.
- **SONG NGỮ NỘI DUNG ĐỀ (chốt 28/08/2026)** — trước đó T5 chỉ dịch nhãn giao diện, đề bài kẹt tiếng Việt. Nay theo đúng khuôn T4: bản VI ở cột gốc, bản EN ở cột **`en_*`** (thiếu thì **rơi về VI**, câu chưa dịch vẫn đọc được), dấu vết bản VI lúc dịch ở **`envi_*`** để biết bản dịch đã cũ. SQL: **`supabase_exam_i18n.sql`** (thêm cột cho `exam_topics`/`exam_questions`/`exam_answers` + thay 4 RPC `exam_boot`/`exam_start`/`exam_submit`/`exam_my_list` để chụp & trả kèm bản EN).
  - Helper trong `exam.js` cạnh `topicById`: `EX.en()` · `EX.tx(o,f)` (chuỗi) · `EX.imgOf(o,f)` (ảnh) · `EX.isStale(o,f)`. Dùng ở **mọi** chỗ render nội dung: chip chủ đề, màn làm bài, Bài Của Tôi, ngân hàng câu hỏi, khung chấm điểm. `setLang` (i18n.js) nay gọi thêm `EX.render()` để đổi bản ngay (câu đang gõ nằm trong `EX.run.answers` nên không mất).
  - **Ảnh riêng bản EN**: cột `exam_questions.en_image_url` (+ `exam_answers.en_image_url` là bản chụp lúc bốc đề). Tải trực tiếp ở từng thẻ trong ngân hàng: `EX.pickEnImg/onEnImgFile/clearEnImg`, input ẩn `#exEnImgFile`. ⚠ Cùng luật với ảnh gốc: **KHÔNG xoá file trên Storage** khi đổi/gỡ (bài đã nộp còn trỏ tới), chỉ gỡ liên kết.
  - **Nhập bản dịch bằng FILE JSON** (`EX.exportTrans`/`importTrans`/`onTransFile`, nút ở card "Bản dịch tiếng Anh" trong Quản Lý Đề, input ẩn `#exTransFile`). File `{v,at,topics:[{id,rows}],questions:[{id,topic,level,rows}]}`, mỗi row `{p:'name'|'question'|'answer',vi,en,stale}`. ⚠ Import **chỉ ghi `en_*`/`envi_*`**, không bao giờ đụng cột VI — đây đúng chỗ T4 từng ghi tiếng Anh đè lên bản gốc (24/08/2026). Ô soạn câu hỏi cũng LUÔN ghi bản VI kể cả khi giao diện đang bật EN (có dòng nhắc màu vàng). Thẻ câu hỏi có chip **Chưa dịch / Đã dịch / Bản dịch đã cũ**.
  - Ô "Dán hàng loạt từ Excel" giữ nguyên 5 cột VI (không có cột EN) — dịch bằng file JSON.
- i18n: nhãn trong `I18N.EN` + `I18N.RX`; tên chủ đề / câu hỏi / đáp án / tên tài khoản gắn `data-noi18n`. ⚠ Khoá **"1 điểm" đã có sẵn** ở khối trên (~dòng 329) — thêm lại là ĐÈ âm thầm. Đã quét EN toàn `#t5` (cả 5 tab nhỏ + màn làm bài) → 0 chữ Việt sót.

## Backend (user tự chạy/deploy)
- `supabase_setup.sql` — bảng `reports`, RLS cơ bản.
- `supabase_auth_setup.sql` — Auth/profiles.
- `supabase_update2.sql` — `my_role`, `change_password`, `audit_log`.
- `supabase_update3.sql` — `login_security`, `login_fail/is_locked/set_lock/login_ok/get_whiteip`.
- `supabase_update4.sql` — `can_edit`, `can_write_report`, RLS ghi theo quyền.
- `supabase_remind_setup.sql` — Nhắc Nhở Telegram: bảng `tg_remind_sent`/`tg_remind_log`, quyền ghi `tgremind`, cron job 1 phút.
- `supabase/functions/tg-remind/index.ts` — deploy tên **`tg-remind`**, BẮT BUỘC `--no-verify-jwt`. Động cơ nhắc nhở (xem mục "NHẮC NHỞ TELEGRAM"). Secret thêm: `TG_CRON_KEY`.
- `supabase_exam_setup.sql` — Kiểm Tra Nghiệp Vụ (T5): 7 bảng `exam_*` + RLS + 8 RPC `exam_*`. Xem mục "T5 — KIỂM TRA NGHIỆP VỤ". **Không cần deploy Edge Function nào cho tab này.**
- `supabase_exam_storage.sql` — bucket `baitest` (private) + 4 policy cho ảnh câu hỏi T5. Chạy SAU file trên.
- `supabase_exam_i18n.sql` — song ngữ nội dung đề T5: thêm cột `en_*`/`envi_*` cho `exam_topics`/`exam_questions`/`exam_answers` + thay 4 RPC (`exam_boot`, `exam_start`, `exam_submit`, `exam_my_list`). Chạy SAU 2 file trên; chạy lại nhiều lần vô hại.
- `supabase_exam_update1.sql` — RPC `exam_delete(uuid)` (TT trở lên) để xóa bài đã nộp; `exam_answers` xóa theo cascade, bài biến mất khỏi xếp hạng, **KHÔNG hoàn lượt test**. UI: nút × trên mỗi dòng rail + nút "Xóa bài này" trong khung chấm (`EX.delSub`).
- `supabase/functions/tg-webhook/index.ts` — deploy tên **`super-function`**. Xử lý: webhook callback (cộng điểm, rid dùng-1-lần), `send_report` (multipart, server chấm điểm), `reset_2fa`, `delete_user`. **Tất cả bí mật ở secret Supabase, KHÔNG hardcode (repo public)**: `TG_BOT_TOKEN` (token bot), `TG_SECRET` (webhook), `TG_G1` (nhóm hậu đài — cược bất thường, chỉ text), `TG_G2` (nhóm xử lý bất thường — text+file/ảnh+nút xác nhận), `TG_G3` (nhóm đại lý ngoài — file báo cáo đại lý). Route: `mode==='cuoc'` → text vào G1 + text/file vào G2; `mode==='dai_ly'` → file vào G3.
- **Múi giờ chấm điểm Bất Thường = GMT+7 (chốt 13/08/2026)**: `sendReport` (index.ts ~107) đóng dấu `isoDate`/`dateStr` bằng `new Date(Date.now()+7*3600000)` + `getUTC*` (Deno Deploy chạy UTC, không tin múi giờ máy chủ). Trước đây dùng `new Date()`+`getDate()` = UTC ⇒ báo cáo gửi **00:00–06:59 giờ VN bị đóng dấu ngày hôm trước**; rơi đúng ngày 1 thì điểm lạc sang **tháng trước**, biến mất khỏi bảng tháng này. Nhánh callback (`cf`) lấy ngày từ `parts[2]` nên tự hưởng, KHÔNG sửa gì thêm. Điểm đã lưu trước đó không đổi. Cần deploy lại `super-function`. ⚠ Khác với **Duyệt Đơn/KM chia ngày theo GMT-4** (giờ gốc cột T file Excel — CỐ Ý giữ, đổi sẽ lệch toàn bộ lịch sử so với file nguồn); riêng KM view Theo Ngày lại dùng GMT+7 (`day_gmt7`, upload.js ~189) trong khi `day_scores` dùng GMT-4 — CHƯA thống nhất.
- **Giới hạn ID Telegram được bấm nút Xác Nhận (chốt 02/08/2026)**: chỉ những Telegram user-ID chỉ định mới bấm được nút `cf/wt/dm` trong callback. `loadConfirmers()` (index.ts) gộp danh sách từ **DB** (`reports` type `tgadmins` month `all`, `{list:[{id,name}]}` — nguồn chính, Admin quản lý qua dashboard) + secret dự phòng `TG_CONFIRMERS` (ID cách nhau dấu phẩy). Danh sách RỖNG ở cả 2 nguồn = cho phép tất cả (tương thích ngược). ID không có trong danh sách → chặn + ghi `audit_log` "Từ chối xác nhận". **UI**: tab Quản Trị (ADMIN-only) card `#admTgCard` — `admLoadTgIds/admAddTgId/admDelTgId` (admin.js) thêm/xóa ID (kèm tên gợi nhớ), lưu `SB.saveReport('tgadmins','all',{list})`. RLS: `is_admin()` toàn quyền nên KHÔNG cần SQL mới. ⚠ ID thật để trong DB (RLS bảo vệ), KHÔNG bao giờ ghi vào repo. Sửa xong phải deploy lại `super-function`.

Bảng `reports(type,month,data JSON)`. type: `don, km, shift, anomaly, work, limits, ov, suspects, whiteip, rids, bc, roster, tgadmins, sop_index, sop_item, tgremind, tgremind_tick`. **`roster`**: month cố định `'all'`, data `{members:[...]}` = danh sách nhân viên dùng chung (xem "Danh sách nhân viên (ROSTER)"). **`tgadmins`**: month `'all'`, data `{list:[{id,name}]}` = ID Telegram được phép bấm nút xác nhận (xem `super-function`).

## Việc đang dở / chờ user
- ~~Gửi Nghi Ngờ → Google Sheet~~: **XONG** — user đã deploy Apps Script, URL đã điền vào `BC.GS_WEBAPP_URL`, endpoint test OK. (Nếu user sửa code .gs phải deploy lại: Deploy → Manage deployments → Edit → New version.)
- ~~Báo Cáo Đơn Rút~~: **XONG** — chuyển sang module `DR` session-only (không cloud, F5 mất), port đầy đủ PROMAX (sửa mốc tiền, sort, cấp độ, drill-down, thả/dán/chọn file riêng). "Vượt hạn mức" đọc từ `DR.data`.
- **Bot lắng nghe thụ động** (đọc text nhóm, nhận ID tài khoản rút ≥150k): CHỜ user cung cấp (a) mẫu/định dạng ID đăng nhập trong text, (b) group ID Telegram, (c) tắt privacy mode qua BotFather `/setprivacy`.
- **Chuyển bot + nhóm sang PRODUCTION (chốt 31/07/2026)**: đã đổi từ bot/nhóm test sang thật. Code đọc ID nhóm từ secret `TG_G1/TG_G2/TG_G3` (không còn hardcode). CHỜ user: (a) set 4 secret `TG_BOT_TOKEN`+`TG_G1/G2/G3` trong Supabase, (b) deploy lại `super-function`, (c) setWebhook lại cho bot mới (kèm `TG_SECRET`), (d) thêm bot vào cả 3 nhóm, (e) `/revoke` token bot cũ (và token production đã lỡ dán trong chat) qua BotFather. ⚠ Nhóm G3 hiện là nhóm THƯỜNG (id dạng ngắn) — nếu bị nâng thành supergroup thì id đổi sang `-100...`, phải cập nhật secret `TG_G3`.
- Nhắc user: xóa điểm test (JADE ngày 15, "Đại lý ngoài"/mkt, tháng 7 = +3); deploy lại Edge Function cho `reset_2fa`; chạy `supabase_update4.sql`; thu hồi token bot cũ qua BotFather.
